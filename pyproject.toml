[tool.pytest.ini_options]
# Make `from tests.schemas.conftest import ...` resolve when pytest runs
# from either the work-spec/ submodule root or the parent formspec monorepo.
pythonpath = ["."]
testpaths = ["tests"]
