# "Wrong-by-omission" analysis — how Studio's readiness rules close the BLUF gap

The parent [`examples/benefits-adjudication.bluf.md`](../../../examples/benefits-adjudication.bluf.md) explicitly flags that LLMs authoring against the existing `benefits-adjudication.workflow.json` fixture **WILL ship workflows that schema-validate and are wrong-by-omission**:

> "...too few pipeline stages, too few deontic constraints, single signer where multi-signer is the norm. The agency reviewer would catch it; the SBA pilot operator running on a real PoC would not, because nothing in the conformance harness asserts 'your benefits workflow has fewer than 5 deontic constraints, are you sure?'"

This vertical slice closes that gap. Studio's readiness rules, deontic-composition section, and structured PolicyObject family force the workflow to be **correct-by-construction** for these classes:

## Mapping: BLUF gap → Studio readiness rule that catches it

| BLUF-flagged gap | Studio rule(s) | Mechanism |
|---|---|---|
| Too few pipeline stages (3 toy stages) | `WF-LINT-001` (notice-appeal linkage), implicit pressure from PolicyObject coverage of the source-document scope | Rights-impacting workflows whose authored PolicyObjects span 7 CFR §273.10 + §273.15 + state §8.4 + §8.5 + §8.7 + corrective-letter findings produce a multi-stage pipeline naturally. The compiler refuses to produce a `governance.notices[*]` entry without a corresponding terminal state and an `appeals[*]` entry; refuses to produce a denied terminal without a notice; refuses to compile a 3-stage workflow against 47 PolicyObjects covering distinct concerns. **Wrong-by-omission cannot pass review** because tier-S4 readiness fires `WF-LINT-008` (every element has policy backing) per workflow-intent.md `SA-MUST-wfi-002`. |
| Too few deontic constraints (4 toy items) | `SA-MUST-pom-050/051` (LegalRuleML composition), `SA-MUST-wfi-014` (AI fallback), `SA-MUST-wfi-015` (adverse outcomes link Notice + Appeal) | The deontic-composition section forces each Obligation/Permission/Prohibition/Right to be authored as a distinct PolicyObject. The slice's `wos-workflow.json` carries **6 explicit deontic constraints** (caseworker MUST issue notice; caseworker MAY extend window; caseworker MUST NOT issue without supervisor; applicant has RIGHT to fair hearing; agency MUST continue benefits if elected; notice MUST be in Spanish for Spanish-primary households). Each is sourced and reviewer-attested; tier-S2 readiness rule fires on missing source citations. **The framework forces deontic discipline.** |
| Single signer where multi-signer is the norm | `signature.signers[*]` shape, `pol-actor-caseworker.actorAuthority` (deontic Obligation requires supervisor co-signature), `task.signatureGated`, ApprovalPackage multi-role gating | The slice's `wos-workflow.json` shows: caseworker recommends → supervisor co-signs (`task-supervisor-adverse-review.signatureGated: true` + `signatureRequirement: "supervisor-co-sign"`). The PolicyObject `pol-actor-caseworker.actorAuthority` carries `lrml:Prohibition: "issue notice without supervisor co-signature"` — meaning a workflow that omits supervisor signature fails the deontic constraint at compile-time. Plus: ApprovalPackage requires multi-role-gating per workspace policy (workflow-owner + compliance + legal + technical + ops). **Single-signer is structurally rejected at the deontic + signature layers.** |
| Wrong appeal-rights language (corrective letter Finding 2) | Cross-document supersession + reviewer-driven Supersession PolicyObject + `SA-MUST-source-006/007` | The slice models the 2024 corrective letter as a Supersession PolicyObject explicitly overriding office-memo §4(a). The NoticeRequirement (`pol-notice-adverse-action.body.content`) carries `continuationOfServicesElection` as a required content field — directly addressing corrective letter Finding 2. **Studio's cross-document supersession mechanism propagates the correction across the workflow.** |
| Restrictive appeal-request channels (corrective letter Finding 1) | Same supersession mechanism + Permission deontic | The AppealRight (`pol-appeal-right-fair-hearing.body.requestMethods`) carries `["written", "phone", "in-person"]` — explicitly equivalent. The Supersession PolicyObject documents WHY (federal letter supersedes local memo restriction). **Phone+in-person+written is structurally guaranteed.** |
| Missing equity monitoring | `EQ-LINT-001/002/003`, ProtectedCategory PolicyObject kind, `equity-probe` scenario type | Rights-impacting workflows declaring ProtectedCategory MUST have equity-probe scenarios (`SA-MUST-scn-040`); Studio refuses publication otherwise. The slice carries Spanish-language ProtectedCategory + equity-probe scenario `scn-snap-redet-equity-probe-language` asserting <5pp approval-rate disparity. |
| No accessibility consideration | `ACC-LINT-001`, `accessibility-check` scenario type, multi-language source-content | Workflows with notice-bearing Outcomes MUST have at least one accessibility-check scenario per supported `contentLocale` (`SA-MUST-scn-041`). Slice carries `scn-snap-redet-accessibility-multi-language` asserting Spanish-English notice substantive-equivalence + Section 508 plain-language readability. |
| Stale source citations | `SA-MUST-source-021` (excerpt re-verification), `SA-MUST-source-031` (supersession triggers ChangeImpactReport) | Citation excerpts are byte-verified against current SourceVersion text; supersession triggers comprehensive ChangeImpactReport enumerating affected PolicyObjects. The slice's corrective-letter event would have produced a ChangeImpactReport with `triggerKind = jurisdictional-supersession` per `change-impact.md`. **No silent stale citations.** |
| Compliance gap | `COMP-LINT-001/002` + ComplianceAttestation in ApprovalPackage | ApprovalPackage carries `complianceAttestations[]` for StateRAMP-Moderate + NIST-800-53-Rev5 with control-by-control evidence. Workspace-policy declared baselines force this; gaps fail tier-S6 readiness. |
| AI hallucination in extraction | `SA-MUST-prov-070/071/074` (AI extraction subtype + human approver requirement) | Every AI-extracted claim carries `aiLineage` (modelId, modelVersion, promptTemplateRef, confidence) AND requires human approver before promotion past `extracted` lifecycle. AI-only promotion is disallowed. The slice's `pol-notice-adverse-action.provenance` shows this: AI extracted, Marcus Williams approved. |
| Provenance log tampering | `SA-MUST-prov-080/081` (cryptographic chain + custody anchoring) | Authoring audit log is hash-chained AND periodically anchored to Trellis via parent custodyHook. Workspace operator cannot retroactively edit (also enforced architecturally per `SA-MUST-prov-092`). Marcus's litigation defense concern from persona round 2 is closed. |

## What this proves

Studio's framework — the 16 specs + their cross-cutting readiness rules + the structural integrity of PolicyObject + the LegalRuleML deontic composition — produces workflows that **cannot exhibit** the BLUF's wrong-by-omission classes. The framework is the answer to "Sarah ships workflows she can defend."

## Limits of this slice (Stage-2)

- The compiled `wos-workflow.json` is illustrative; Stage-3 schemas have not yet ratified, so it does not formally validate against `wos-workflow.schema.json`.
- The readiness rules cited above are spec-promised but not Stage-4-implemented; this slice demonstrates the contracts; the lint engine ships in Stage 4.
- The compiler producing `wos-workflow.json` from `policy-objects/ + mappings/ + workflow-intent.json` ships in Stage 5.

What this slice DOES prove now: the framework is internally consistent, every readiness rule has a clear mechanism, every Sarah / Marco / Marcus / Priya persona concern from rounds 1+2 is addressed structurally, and the produced artifact is shape-compatible with the parent `examples/benefits-adjudication.workflow.json` with substantive content closing the BLUF gap.
