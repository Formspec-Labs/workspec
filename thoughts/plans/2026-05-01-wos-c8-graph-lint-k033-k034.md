# C8 follow-up — graph membership lint (K-033 / K-034)

**Status:** Scoped out of [`2026-05-01-wos-runtime-parity-and-vocab-closure.md`](./2026-05-01-wos-runtime-parity-and-vocab-closure.md) §C8 so the parity plan can close while this work streams independently.

**Goal:** Tier-1 `wos-lint` rules JSON Schema cannot express:

- Transition `target` resolves to a declared state (or compound substate path).
- Compound / parallel-region join targets resolve consistently with kernel topology.

**Deliverables:**

- Rule identifiers **K-033** / **K-034** (or split further if scope demands), with normative spec citations in rule messages.
- Rows in `crates/wos-lint/LINT-MATRIX.md` and tests under `crates/wos-lint/tests/` (or an extension of `tier1_rules.rs`).

**Non-goals (here):** Changing kernel JSON Schema to encode topology beyond what K-031/K-032 already cover.

**Verification:** `cargo test -p wos-lint` for new tests; existing `tier1_rules` gates stay green.
