{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://wos-spec.org/schemas/due-process/1.0",
  "title": "WOS Due Process Config",
  "description": "A WOS Due Process Config sidecar document. Provides detailed due process configuration -- notice templates, explanation templates, appeal routing, continuation-of-service policies -- for a WOS Workflow Governance Document. Operational parameters can be updated independently of the governance structure.",
  "type": "object",
  "required": [
    "$wosDueProcess",
    "targetGovernance"
  ],
  "patternProperties": {
    "^x-": {
      "$comment": "Vendor extensions per Kernel spec §10.6 (extensions escape hatch)"
    }
  },
  "additionalProperties": false,
  "properties": {
    "$wosDueProcess": {
      "type": "string",
      "const": "1.0",
      "description": "Document type marker identifying this as a WOS Due Process Config sidecar document (Governance S3). MUST be the string '1.0'. Processors use this field to dispatch to the due process config parser; its absence means the document is not a recognized Due Process Config.",
      "examples": ["1.0", "1.0"],
      "x-lm": {
        "critical": true,
        "intent": "Document type marker for WOS Due Process Config sidecar"
      }
    },
    "$schema": { "$ref": "#/$defs/JsonSchemaUri" },
    "targetGovernance": {
      "type": "string",
      "format": "uri",
      "description": "URI of the WOS Workflow Governance Document this Due Process Config targets. Must match the governance document's url field exactly. Processors MUST reject a Due Process Config whose targetGovernance does not match the governance document being loaded (Governance S3.1).",
      "examples": [
        "https://agency.gov/governance/benefits-adjudication-governance",
        "https://procurement.example.gov/governance/purchase-order-governance"
      ],
      "x-lm": {
        "critical": true,
        "intent": "Binds this sidecar to its parent governance document"
      }
    },
    "version": {
      "type": "string",
      "minLength": 1,
      "description": "Semantic version of this Due Process Config document. Useful for audit trails — allows governance documents to pin a specific due process configuration.",
      "examples": ["1.0.0", "2025.3.0"]
    },
    "noticeTemplates": {
      "type": "array",
      "items": {
        "$ref": "#/$defs/NoticeTemplate"
      },
      "description": "Templates for adverse decision notices."
    },
    "explanationTemplates": {
      "type": "array",
      "items": {
        "$ref": "#/$defs/ExplanationTemplate"
      },
      "description": "Templates for decision explanations at various levels."
    },
    "appealRouting": {
      "$ref": "#/$defs/AppealRouting",
      "description": "Detailed appeal routing configuration."
    },
    "continuationPolicies": {
      "type": "array",
      "items": {
        "$ref": "#/$defs/ContinuationPolicy"
      },
      "description": "Continuation-of-service policies for different adverse decision types."
    },
    "extensions": { "$ref": "#/$defs/ExtensionsMap" }
  },
  "$defs": {
    "NoticeTemplate": {
      "type": "object",
      "required": ["id", "title", "sections"],
      "patternProperties": {
        "^x-": {
          "$comment": "Vendor extensions per Kernel spec §10.6"
        }
      },
      "additionalProperties": false,
      "properties": {
        "id": {
          "type": "string",
          "minLength": 1,
          "description": "Unique identifier for this notice template within the Due Process Config. Referenced by AdverseDecisionPolicy.noticeTemplateRef in the parent governance document.",
          "examples": ["adverseDecisionNotice", "appealAcknowledgment"]
        },
        "title": {
          "type": "string",
          "description": "Notice title displayed to the recipient. Used as the document heading in rendered notice output.",
          "examples": ["Notice of Adverse Determination", "Acknowledgment of Appeal Filing"]
        },
        "sections": {
          "type": "array",
          "items": { "type": "string", "minLength": 1, "description": "Named section that MUST appear in notices generated from this template.", "examples": ["determination", "factualBasis", "appealRights", "deadline"] },
          "description": "Named sections that MUST appear in notices generated from this template. Section names are workflow-defined; the processor validates that each section is present in the rendered output.",
          "examples": [["determination", "factualBasis", "appealRights", "deadline"], ["summary", "regulatoryBasis", "nextSteps"]]
        },
        "formspecDefinitionRef": {
          "type": "string",
          "format": "uri",
          "description": "Optional URI reference to a Formspec Definition that provides structured rendering for this notice. When present, the processor uses the Formspec form as the layout for the notice.",
          "examples": ["https://agency.gov/forms/notices/adverse-decision/v1.0", "https://agency.gov/forms/notices/appeal-ack/v2.0"]
        },
        "description": {
          "type": "string",
          "description": "Human-readable description of when this notice template is used and what it communicates to the recipient.",
          "examples": ["Sent to applicants before an adverse benefits determination takes effect, per APA notice requirements.", "Acknowledgment sent when an appeal is filed, confirming receipt and continuation of services."]
        }
      }
    },
    "ExplanationTemplate": {
      "type": "object",
      "required": ["id", "level"],
      "patternProperties": {
        "^x-": {
          "$comment": "Vendor extensions per Kernel spec §10.6"
        }
      },
      "additionalProperties": false,
      "properties": {
        "id": {
          "type": "string",
          "minLength": 1,
          "description": "Unique identifier for this explanation template within the Due Process Config. Referenced by explanation generation logic in the processor.",
          "examples": ["individualizedExplanation", "categoricalExplanation"]
        },
        "level": {
          "type": "string",
          "enum": ["individualized", "categorical", "aggregate"],
          "description": "Explanation level this template serves (Governance S3.3). 'individualized': REQUIRED for rights-impacting decisions. 'categorical': RECOMMENDED for operational decisions. 'aggregate': minimum for informational decisions.",
          "examples": ["individualized", "categorical"]
        },
        "requiredElements": {
          "type": "array",
          "items": { "type": "string", "minLength": 1, "description": "Named element that MUST be present in explanations generated from this template.", "examples": ["specificDetermination", "factualBasis", "regulatoryAuthority", "counterfactuals"] },
          "description": "Named elements that MUST be present in explanations generated from this template. Element names are workflow-defined; the processor validates presence in the rendered explanation.",
          "examples": [["specificDetermination", "factualBasis", "regulatoryAuthority", "counterfactuals"], ["summary", "regulatoryBasis"]]
        },
        "description": {
          "type": "string",
          "description": "Human-readable description of this explanation template's purpose and scope. Intended for governance authors and auditors.",
          "examples": ["Individualized explanation for adverse benefits decisions — includes counterfactuals and appeal rights per APA requirements.", "Categorical explanation for routine denials — summarizes applicable rule categories without individual counterfactuals."]
        }
      }
    },
    "AppealRouting": {
      "type": "object",
      "required": ["independenceConstraint"],
      "patternProperties": {
        "^x-": {
          "$comment": "Vendor extensions per Kernel spec §10.6"
        }
      },
      "additionalProperties": false,
      "properties": {
        "defaultReviewerPool": {
          "type": "string",
          "description": "Default pool of actors eligible to review appeals when no specific reviewer is assigned. Referenced by the processor during appeal routing.",
          "examples": ["independentAdjudicators", "externalReviewPanel"]
        },
        "independenceConstraint": {
          "type": "string",
          "description": "How independence from the original determination is ensured for appeal reviewers (Governance S3.5). Processors MUST enforce this constraint during reviewer assignment.",
          "examples": ["differentUnit", "differentAgency", "externalPanel"]
        },
        "escalationPath": {
          "type": "array",
          "items": { "type": "string", "minLength": 1, "description": "Actor pool or role identifier representing one escalation level in the appeal review chain.", "examples": ["supervisorReview", "directorReview", "externalPanel"] },
          "description": "Ordered escalation path for contested appeal reviews. If an appeal reviewer's decision is contested, the processor routes to the next level in this sequence.",
          "examples": [["supervisorReview", "directorReview", "externalPanel"], ["seniorAdjudicator", "commissioner"]]
        }
      }
    },
    "ContinuationPolicy": {
      "type": "object",
      "required": ["decisionType", "scope"],
      "patternProperties": {
        "^x-": {
          "$comment": "Vendor extensions per Kernel spec §10.6"
        }
      },
      "additionalProperties": false,
      "properties": {
        "decisionType": {
          "type": "string",
          "description": "Type of adverse decision this continuation-of-service policy applies to. Matches the workflow's adverse decision classification vocabulary.",
          "examples": ["benefitTermination", "benefitReduction", "applicationDenial"]
        },
        "scope": {
          "type": "string",
          "description": "Scope of services maintained during the appeal window under this policy. Determines what the individual continues to receive while their appeal is pending.",
          "examples": ["currentLevel", "reducedLevel", "emergencyOnly"]
        },
        "duration": {
          "type": "string",
          "description": "ISO 8601 duration for which services are continued pending appeal resolution. When the appeal window expires without resolution, the processor applies the configured timeout action.",
          "examples": ["P30D", "P90D", "P14D"]
        },
        "description": {
          "type": "string",
          "description": "Human-readable description of this continuation-of-service policy and its regulatory basis. Intended for governance authors and auditors.",
          "examples": ["Benefit termination: current-level continuation for 30 days pending appeal, per APA 5 USC 554.", "Benefit reduction: continue at current level for 14 days to allow appeal filing."]
        }
      }
    }
  ,
    "ExtensionsMap": {
      "type": "object",
      "description": "Vendor extension data attached to this node. All keys MUST start with 'x-' (see Kernel §10.6). The reserved namespace 'x-wos-*' is for WOS Working Group use only; third-party extensions MUST use a unique vendor prefix (e.g., 'x-acme-', 'x-vendor-'). Processors MUST ignore unknown extension keys to preserve forward compatibility. Extension values are unconstrained — any JSON value is valid, but authors are encouraged to document the shape in their vendor spec.",
      "examples": [
        { "x-acme-audit-ref": "AUD-2026-001" },
        { "x-vendor-tier": "gold", "x-vendor-region": "us-east" }
      ],
      "propertyNames": { "pattern": "^x-" },
      "additionalProperties": {}
    },
    "JsonSchemaUri": {
      "type": "string",
      "format": "uri",
      "description": "Optional JSON Schema URI enabling editor validation and autocompletion. When present, editors (VS Code, IntelliJ, etc.) validate the document against this schema. Omit in production documents; the authoritative schema URI is derived from the document type marker (e.g., '$wosKernel': '1.0'). Used for author-time tooling only — runtime processors MUST ignore this field.",
      "examples": [
        "https://wos-spec.org/schemas/kernel/wos-kernel.schema.json",
        "https://wos-spec.org/schemas/governance/wos-workflow-governance.schema.json"
      ]
    }
  }
}
