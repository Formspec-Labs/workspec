{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://wos-spec.org/schemas/drift-monitor/1.0",
  "title": "WOS Drift Monitor Config",
  "description": "A WOS Drift Monitor Config sidecar document. Provides drift detection and monitoring configuration for agents in a WOS workflow -- drift detection methods, evaluation windows, alert thresholds, rubber-stamp detection, and deployment sequence policies. Monitoring parameters can be tuned independently of the governance document.",
  "type": "object",
  "required": [
    "$wosDriftMonitor",
    "targetWorkflow",
    "monitors"
  ],
  "patternProperties": {
    "^x-": {
      "$comment": "Vendor extensions per Kernel spec §10.6 (extensions escape hatch)"
    }
  },
  "additionalProperties": false,
  "properties": {
    "$wosDriftMonitor": {
      "type": "string",
      "const": "1.0",
      "description": "WOS Drift Monitor Config specification version marker. MUST be '1.0'. Identifies this document as a WOS Drift Monitor Config sidecar and pins the schema version. Processors MUST reject documents with an unrecognized value. See AI Integration §9.",
      "examples": ["1.0", "1.0"],
      "x-lm": {
        "critical": true,
        "intent": "Document type marker for WOS Drift Monitor Config sidecar"
      }
    },
    "$schema": { "$ref": "#/$defs/JsonSchemaUri" },
    "targetWorkflow": {
      "type": "string",
      "format": "uri",
      "description": "URI of the WOS Kernel Document whose AI agents this monitor tracks. MUST match the 'url' property of the target Kernel Document. Processors use this to correlate drift events with the correct workflow governance and invoke demotion rules when thresholds are crossed. See AI Integration §9.",
      "examples": ["https://agency.gov/workflows/benefits-adjudication", "https://agency.gov/workflows/grants-review"],
      "x-lm": {
        "critical": true,
        "intent": "Binds this sidecar to the monitored workflow"
      }
    },
    "version": {
      "type": "string",
      "minLength": 1,
      "description": "Version of this Drift Monitor Config document. RECOMMENDED to use semantic versioning (major.minor.patch) so monitoring configuration changes are independently tracked in audit records.",
      "examples": ["1.0.0", "2.3.1"]
    },
    "title": {
      "type": "string",
      "description": "Human-readable name for this drift monitor configuration. Displayed in administrative dashboards and alert notifications so operators can quickly identify which monitor fired.",
      "examples": ["Benefits Adjudication Drift Monitor", "Grants Review AI Monitoring Config"]
    },
    "monitors": {
      "type": "array",
      "items": { "$ref": "#/$defs/AgentMonitor" },
      "minItems": 1,
      "description": "Per-agent monitoring configurations.",
      "examples": [
        [{ "agentRef": "documentExtractor", "metrics": [{ "name": "extractionAccuracy", "method": "accuracy", "threshold": 0.90, "windowSize": "P30D" }] }]
      ],
      "x-lm": {
        "critical": true,
        "intent": "Defines what to monitor for each agent and when to alert"
      }
    },
    "deploymentSequence": {
      "$ref": "#/$defs/DeploymentSequence",
      "description": "Shadow/canary/production deployment sequence policy. RECOMMENDED for rights-impacting and safety-impacting workflows."
    },
    "extensions": { "$ref": "#/$defs/ExtensionsMap" }
  },
  "$defs": {
    "AgentMonitor": {
      "type": "object",
      "required": ["agentRef", "evaluationWindow", "metrics"],
      "patternProperties": {
        "^x-": {
          "$comment": "Vendor extensions per Kernel spec §10.6"
        }
      },
      "additionalProperties": false,
      "properties": {
        "agentRef": {
          "type": "string",
          "minLength": 1,
          "description": "Agent identifier to monitor. MUST match the 'id' of an AgentDeclaration in the target AI Integration Document. All metric evaluations and alert thresholds defined under this monitor apply exclusively to this agent's invocations.",
          "examples": ["documentExtractor", "eligibilityScreener"]
        },
        "evaluationWindow": {
          "type": "string",
          "description": "ISO 8601 duration of the rolling evaluation window over which metrics are computed. Longer windows smooth short-term variance; shorter windows detect rapid drift earlier. Set shorter windows for high-impact agents.",
          "examples": ["P30D", "P7D"]
        },
        "metrics": {
          "type": "array",
          "items": { "$ref": "#/$defs/MonitorMetric" },
          "minItems": 1,
          "description": "Metrics to track for this agent."
        },
        "rubberStamp": {
          "$ref": "#/$defs/RubberStampConfig",
          "description": "Rubber-stamp detection for human reviewers of this agent's output."
        },
        "alertThresholds": {
          "type": "array",
          "items": { "$ref": "#/$defs/AlertThreshold" },
          "description": "Conditions that generate alerts or trigger automatic governance actions."
        }
      }
    },
    "MonitorMetric": {
      "type": "object",
      "required": ["name"],
      "patternProperties": {
        "^x-": {
          "$comment": "Vendor extensions per Kernel spec §10.6"
        }
      },
      "additionalProperties": false,
      "properties": {
        "name": {
          "type": "string",
          "minLength": 1,
          "description": "Logical name for this metric, used to reference the metric in alert threshold conditions (e.g., 'extractionAccuracy < 0.90'). Choose a name that clearly identifies what is being measured so alert conditions are self-documenting.",
          "examples": ["accuracy", "agreementRate", "confidenceDistribution", "modificationRate", "outputDistribution"]
        },
        "method": {
          "type": "string",
          "enum": ["psi", "ks", "chi2", "accuracy", "custom"],
          "description": "Statistical method for drift detection. 'psi': Population Stability Index. 'ks': Kolmogorov-Smirnov test. 'chi2': Chi-squared test. 'accuracy': accuracy against human-reviewed ground truth. 'custom': implementation-defined method.",
          "examples": ["accuracy", "psi"]
        },
        "threshold": {
          "type": "number",
          "description": "Threshold value for drift detection. Semantics depend on the method.",
          "examples": [0.2, 0.05, 0.92]
        }
      }
    },
    "RubberStampConfig": {
      "type": "object",
      "patternProperties": {
        "^x-": {
          "$comment": "Vendor extensions per Kernel spec §10.6"
        }
      },
      "additionalProperties": false,
      "properties": {
        "enabled": {
          "type": "boolean",
          "default": false,
          "description": "Whether rubber-stamp detection monitoring is active for this agent. When true, the monitor tracks reviewer engagement metrics (minReviewTime and maxAgreementRate) and generates alerts when reviewers appear to approve agent outputs without meaningful scrutiny.",
          "examples": [true, false]
        },
        "minReviewTime": {
          "type": "string",
          "description": "ISO 8601 duration below which a review is flagged as potentially rubber-stamped.",
          "examples": ["PT30S", "PT2M"]
        },
        "maxAgreementRate": {
          "type": "number",
          "minimum": 0,
          "maximum": 1,
          "description": "Agreement rate above which reviewer engagement is flagged as potentially declining. When a reviewer agrees with the agent more than this fraction of the time within the evaluationWindow, the pattern is reported for oversight review.",
          "examples": [0.95, 0.98]
        },
        "evaluationWindow": {
          "type": "string",
          "description": "ISO 8601 duration of the evaluation window for rubber-stamp metrics.",
          "examples": ["P30D", "P7D"]
        }
      }
    },
    "AlertThreshold": {
      "type": "object",
      "required": ["condition", "severity"],
      "patternProperties": {
        "^x-": {
          "$comment": "Vendor extensions per Kernel spec §10.6"
        }
      },
      "additionalProperties": false,
      "properties": {
        "condition": {
          "type": "string",
          "minLength": 1,
          "description": "FEL expression or metric condition that triggers the alert. References metric names defined in the parent AgentMonitor.metrics array. When the expression evaluates to true the threshold is considered crossed and the configured action fires.",
          "examples": ["accuracy < 0.90", "psi > 0.25"]
        },
        "severity": {
          "type": "string",
          "enum": ["info", "warning", "critical"],
          "description": "Alert severity level that controls urgency of notification routing and escalation. 'critical' thresholds SHOULD include an automatic governance action such as 'demoteToAssistive' or 'suspend'.",
          "examples": ["critical", "warning"]
        },
        "action": {
          "type": "string",
          "enum": ["notify", "demoteToAssistive", "demoteToManual", "suspend"],
          "description": "Automatic governance action to take when threshold is crossed. 'notify': alert administrators. 'demoteToAssistive': cap autonomy at assistive. 'demoteToManual': cap autonomy at manual. 'suspend': suspend agent invocations. When 'policyRef' is also present, the named DemotionRule's structured semantics (condition / targetLevel / pendingRecalibration) take precedence over this enum's implementation-defined behavior; this field then serves as a human-readable hint and a fallback when the ref cannot be resolved.",
          "examples": ["demoteToAssistive", "notify"]
        },
        "policyRef": {
          "type": "string",
          "minLength": 1,
          "description": "Reference to a DemotionRule.id declared in an Agent Config sidecar that targets the same workflow as this monitor. When present, the named DemotionRule fires with its full structured semantics (condition / targetLevel / pendingRecalibration / description) instead of the implementation-defined 'action' enum. The ref MUST resolve to a DemotionRule.id reachable through an Agent Config whose targetAgent matches this monitor's parent agentRef; an unresolvable ref MUST emit a configuration warning in provenance and fall back to the 'action' enum behavior. Audit records cite the resolved DemotionRule.id so operators know exactly which named rule fired.",
          "examples": ["demoteOnLowAccuracy", "demoteOnConsecutiveOverrides"],
          "x-lm": {
            "critical": true,
            "intent": "Binds an AlertThreshold to a named, structured DemotionRule in an Agent Config sidecar"
          }
        },
        "notifyRoles": {
          "type": "array",
          "items": {
            "type": "string",
            "minLength": 1,
            "description": "Role identifier to notify when this alert threshold is crossed. MUST match a role declared in the governance document's actorRegistry or a well-known WOS role identifier.",
            "examples": ["systemAdministrator", "programDirector"]
          },
          "description": "Roles to notify when this threshold is crossed.",
          "examples": [["systemAdministrator", "programDirector"]]
        }
      }
    },
    "DeploymentSequence": {
      "type": "object",
      "patternProperties": {
        "^x-": {
          "$comment": "Vendor extensions per Kernel spec §10.6"
        }
      },
      "additionalProperties": false,
      "properties": {
        "enabled": {
          "type": "boolean",
          "default": false,
          "description": "Whether the shadow/canary/production deployment sequence is enforced for this workflow. When true, new agent versions MUST progress through shadow and canary phases before receiving full production traffic.",
          "examples": [true, false]
        },
        "shadowDuration": {
          "type": "string",
          "description": "ISO 8601 duration of the shadow phase, during which the new agent version runs in parallel with the production version but its outputs are not used to make decisions. Drift metrics are collected to verify readiness for canary promotion.",
          "examples": ["P14D", "P7D"]
        },
        "canaryPercentage": {
          "type": "number",
          "minimum": 0,
          "maximum": 1,
          "description": "Fraction of live invocations routed to the canary version during the canary phase. The remainder continues using the current production version. Start conservatively (0.05) and increase only after monitoring confirms acceptable metrics.",
          "examples": [0.05, 0.1]
        },
        "canaryDuration": {
          "type": "string",
          "description": "ISO 8601 duration of the canary phase, during which the new version handles a small percentage of real traffic. At end of canary phase, metrics are reviewed before full promotion to production.",
          "examples": ["P7D", "P14D"]
        },
        "canaryReviewSamplingRate": {
          "type": "number",
          "minimum": 0,
          "maximum": 1,
          "description": "Elevated human review sampling rate applied to canary invocations during the canary phase. Set to 1.0 (100% sampling) for rights-impacting workflows to ensure maximum oversight while evaluating the new model version.",
          "examples": [0.5, 1.0]
        }
      }
    }
  ,
    "ExtensionsMap": {
      "type": "object",
      "description": "Vendor extension data attached to this node. All keys MUST start with 'x-' (see Kernel §10.6). The reserved namespace 'x-wos-*' is for WOS Working Group use only; third-party extensions MUST use a unique vendor prefix (e.g., 'x-acme-', 'x-vendor-'). Processors MUST ignore unknown extension keys to preserve forward compatibility. Extension values are unconstrained — any JSON value is valid, but authors are encouraged to document the shape in their vendor spec.",
      "examples": [
        { "x-acme-audit-ref": "AUD-2026-001" },
        { "x-vendor-tier": "gold", "x-vendor-region": "us-east" }
      ],
      "propertyNames": { "pattern": "^x-" },
      "additionalProperties": {}
    },
    "JsonSchemaUri": {
      "type": "string",
      "format": "uri",
      "description": "Optional JSON Schema URI enabling editor validation and autocompletion. When present, editors (VS Code, IntelliJ, etc.) validate the document against this schema. Omit in production documents; the authoritative schema URI is derived from the document type marker (e.g., '$wosKernel': '1.0'). Used for author-time tooling only — runtime processors MUST ignore this field.",
      "examples": [
        "https://wos-spec.org/schemas/kernel/wos-kernel.schema.json",
        "https://wos-spec.org/schemas/governance/wos-workflow-governance.schema.json"
      ]
    }
  }
}
