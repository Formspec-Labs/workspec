{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://wos-spec.org/schemas/ai-integration/1.0",
  "title": "WOS AI Integration Document",
  "description": "A WOS AI Integration Document per the WOS AI Integration Specification v1.0 (Layer 2). Targets a WOS Kernel Document and declares agent registration, deontic constraints (permission/prohibition/obligation/right), autonomy levels with impact-level caps, Formspec-as-validator pattern, confidence framework with decay, fallback chains, decision drift detection, AI-specific oversight extensions, volume constraints, agent-specific review sampling, agent disclosure, and Narrative provenance tier. Every Layer 2 concept extends a Layer 1 (Workflow Governance) concept through the kernel's named seams. The agent is outside the trust boundary -- the WOS Processor enforces all governance. WOS MUST NOT alter core Formspec processing semantics.",
  "type": "object",
  "required": [
    "$wosAIIntegration",
    "targetWorkflow",
    "agents"
  ],
  "patternProperties": {
    "^x-": {
      "$comment": "Vendor extensions per Kernel spec §10.6 (extensions escape hatch)"
    }
  },
  "additionalProperties": false,
  "properties": {
    "$wosAIIntegration": {
      "type": "string",
      "const": "1.0",
      "description": "WOS AI Integration specification version. MUST be '1.0'. Identifies this document as a WOS AI Integration Document.",
      "examples": [
        "1.0"
      ],
      "x-lm": {
        "critical": true,
        "intent": "Document type marker and version pin for WOS AI Integration documents"
      }
    },
    "$schema": {
      "type": "string",
      "format": "uri",
      "description": "Optional JSON Schema URI for editor validation and autocompletion."
    },
    "targetWorkflow": {
      "type": "string",
      "format": "uri",
      "description": "URI of the WOS Kernel Document this AI integration document targets. MUST match the url property of the target Kernel Document.",
      "examples": [
        "https://agency.gov/workflows/benefits-adjudication"
      ],
      "x-lm": {
        "critical": true,
        "intent": "Binds this AI integration document to a specific kernel workflow"
      }
    },
    "version": {
      "type": "string",
      "minLength": 1,
      "description": "Version of this AI Integration Document.",
      "examples": [
        "1.0.0"
      ]
    },
    "title": {
      "type": "string",
      "description": "Human-readable name for this AI integration configuration."
    },
    "description": {
      "type": "string",
      "description": "Human-readable description of how AI agents participate in this workflow."
    },
    "defaultAutonomy": {
      "type": "string",
      "enum": [
        "autonomous",
        "supervisory",
        "assistive",
        "manual"
      ],
      "default": "manual",
      "description": "Default autonomy level for agent actions in this workflow (AI Integration S5). Autonomy is a property of the action site, not the agent. Individual actions MAY override this default. The effective autonomy MUST NOT exceed the impact-level cap.",
      "examples": [
        "assistive"
      ]
    },
    "agents": {
      "type": "array",
      "items": {
        "$ref": "#/$defs/AgentDeclaration"
      },
      "minItems": 1,
      "description": "Agent declarations registered via the actorExtension seam (AI Integration S3).",
      "examples": [
        [
          {
            "id": "documentExtractor",
            "type": "agent",
            "agentType": "generative",
            "modelIdentifier": "gpt-4o",
            "modelVersion": "2025-03",
            "modelVersionPolicy": "pinned"
          }
        ]
      ],
      "x-lm": {
        "critical": true,
        "intent": "Registers AI agents as actors in the workflow with identity, type taxonomy, and capabilities"
      }
    },
    "deonticConstraints": {
      "$ref": "#/$defs/DeonticConstraints",
      "description": "Deontic constraint framework governing agent behavior (AI Integration S4). Four constraint types from OASIS LegalRuleML: permissions, prohibitions, obligations, rights. Evaluated after contract validation, before output commit. The WOS Processor is the Policy Enforcement Point.",
      "examples": [
        {
          "prohibitions": [
            {
              "id": "noFinalDenial",
              "condition": "output.eligible = false and instance.impactLevel = 'rights-impacting'",
              "reason": "Agent MUST NOT issue final denial on rights-impacting case",
              "onViolation": "escalateToHuman"
            }
          ],
          "obligations": [
            {
              "id": "requireConfidence",
              "requirement": "output.confidence.overall >= 0.85",
              "reason": "Output must meet minimum confidence",
              "onViolation": "reject"
            }
          ]
        }
      ],
      "x-lm": {
        "critical": true,
        "intent": "Structural governance boundary for all agent outputs -- the deontic constraint framework"
      }
    },
    "confidenceFloor": {
      "$ref": "#/$defs/ConfidenceFloor",
      "description": "Workflow-level minimum confidence threshold (AI Integration S7.4). Part of the enforcement ordering (step 4)."
    },
    "fallbackChain": {
      "type": "array",
      "items": {
        "$ref": "#/$defs/FallbackLevel"
      },
      "description": "Default fallback chain for agent invocation failures (AI Integration S8). MUST terminate in escalateToHuman or fail. MUST NOT cycle.",
      "examples": [
        [
          {
            "action": "retry",
            "maxRetries": 2,
            "backoff": "exponential",
            "initialInterval": "PT5S"
          },
          {
            "action": "escalateToHuman",
            "taskRef": "manualReview"
          }
        ]
      ],
      "x-lm": {
        "critical": true,
        "intent": "Guarantees graceful degradation -- every agent failure has a defined resolution path"
      }
    },
    "oversightExtensions": {
      "$ref": "#/$defs/OversightExtensions",
      "description": "AI-specific extensions to Layer 1 review protocols (AI Integration S10). Attach via lifecycleHook on review-tagged transitions."
    },
    "volumeConstraints": {
      "$ref": "#/$defs/VolumeConstraints",
      "description": "Rate limits on autonomous agent actions (AI Integration S11). Part of the enforcement ordering (step 5)."
    },
    "reviewSampling": {
      "$ref": "#/$defs/AgentReviewSampling",
      "description": "Agent-specific review sampling extending Layer 1 quality controls (AI Integration S11.2). Part of the enforcement ordering (step 6)."
    },
    "agentDisclosure": {
      "$ref": "#/$defs/AgentDisclosure",
      "description": "Agent disclosure requirements extending Layer 1 due process (AI Integration S12). Attach via lifecycleHook on adverse-decision-tagged transitions."
    },
    "narrativeTier": {
      "$ref": "#/$defs/NarrativeTierConfig",
      "description": "Configuration for the Narrative provenance tier (AI Integration S13). Attach via provenanceLayer seam."
    },
    "driftDetection": {
      "$ref": "#/$defs/DriftDetectionConfig",
      "description": "Decision drift detection configuration (AI Integration S9)."
    },
    "assistGovernanceProxy": {
      "$ref": "#/$defs/AssistGovernanceProxy",
      "description": "WOS Assist Governance Proxy configuration (AI Integration S14). A WOS construct consuming the Formspec Assist protocol (Assist S2.1)."
    },
    "extensions": {
      "type": "object",
      "propertyNames": {
        "pattern": "^x-"
      },
      "description": "Extension data. All keys MUST be prefixed with 'x-'."
    }
  },
  "$defs": {
    "AgentDeclaration": {
      "type": "object",
      "required": [
        "id",
        "type",
        "agentType",
        "modelIdentifier",
        "modelVersion"
      ],
      "additionalProperties": false,
      "properties": {
        "id": {
          "type": "string",
          "minLength": 1,
          "description": "Unique agent identifier within the workflow.",
          "examples": [
            "documentExtractor",
            "eligibilityScreener",
            "caseClassifier"
          ]
        },
        "type": {
          "type": "string",
          "const": "agent",
          "description": "Actor type. MUST be 'agent' for agent declarations.",
          "examples": [
            "agent"
          ],
          "x-lm": {
            "critical": true,
            "intent": "Registers this actor as an agent via the kernel actorExtension seam"
          }
        },
        "agentType": {
          "type": "string",
          "enum": [
            "deterministic",
            "statistical",
            "generative"
          ],
          "description": "Agent type taxonomy (AI Integration S3.2). 'deterministic': rules engines, output is a pure function of input. 'statistical': classical ML, bounded output space, calibratable. 'generative': LLMs, unbounded output, non-deterministic, injection-vulnerable.",
          "examples": [
            "generative",
            "statistical",
            "deterministic"
          ],
          "x-lm": {
            "critical": true,
            "intent": "Governs the applicable governance regime -- generative agents require the strongest controls"
          }
        },
        "modelIdentifier": {
          "type": "string",
          "minLength": 1,
          "description": "Identifier of the model powering this agent.",
          "examples": [
            "claude-sonnet-4-20250514",
            "gpt-4o-2024-11-20",
            "custom-doc-extractor-v3"
          ]
        },
        "modelVersion": {
          "type": "string",
          "minLength": 1,
          "description": "Version of the model.",
          "examples": [
            "2.0.0",
            "2025-05-14"
          ]
        },
        "description": {
          "type": "string",
          "description": "Human-readable description of the agent's role in the workflow."
        },
        "capabilities": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/Capability"
          },
          "description": "Declared capabilities with I/O contracts (AI Integration S3.3)."
        },
        "modelVersionPolicy": {
          "type": "string",
          "enum": [
            "pinned",
            "approved",
            "latest"
          ],
          "default": "pinned",
          "description": "Model version policy (AI Integration S3.4). 'pinned': exactly the declared version. 'approved': any version from an approved list. 'latest': latest available, version recorded in provenance.",
          "examples": [
            "pinned"
          ]
        },
        "confidenceDecay": {
          "$ref": "#/$defs/ConfidenceDecay",
          "description": "Agent-specific confidence decay configuration (AI Integration S7.5)."
        },
        "fallbackChain": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/FallbackLevel"
          },
          "description": "Agent-specific fallback chain. Overrides the workflow-level fallback chain for this agent."
        },
        "cascadingInvocations": {
          "type": "array",
          "items": {
            "type": "string",
            "minLength": 1
          },
          "description": "Agent IDs that this agent may invoke at autonomous level (AI Integration S3.7). Required when cascading autonomous agents are used.",
          "examples": [
            [
              "postProcessor",
              "qualityChecker"
            ]
          ]
        },
        "extensions": {
          "type": "object",
          "propertyNames": {
            "pattern": "^x-"
          },
          "description": "Extension data for this agent declaration."
        },
        "deonticConstraints": {
          "$ref": "#/$defs/DeonticConstraints",
          "description": "Agent-level deontic constraints (AI Integration S4.7). Apply to all invocations of this agent."
        }
      }
    },
    "Capability": {
      "type": "object",
      "required": [
        "id"
      ],
      "additionalProperties": false,
      "properties": {
        "id": {
          "type": "string",
          "minLength": 1,
          "description": "Capability identifier.",
          "examples": [
            "documentExtraction",
            "eligibilityPreScreen",
            "incomeVerification"
          ]
        },
        "description": {
          "type": "string",
          "description": "What this capability does."
        },
        "inputContractRef": {
          "type": "string",
          "format": "uri",
          "description": "Formspec Definition or JSON Schema URI for input. Uses headless contract pattern (Core S2.3).",
          "examples": [
            "urn:formspec:agency.gov:doc-extraction-input:1.0.0"
          ]
        },
        "outputContractRef": {
          "type": "string",
          "format": "uri",
          "description": "Formspec Definition or JSON Schema URI for output. Agent output is validated against this contract (Formspec-as-validator, S6).",
          "examples": [
            "urn:formspec:agency.gov:doc-extraction-output:1.0.0"
          ]
        }
      }
    },
    "DeonticConstraints": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "permissions": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/Permission"
          },
          "description": "Permissions bounding what agents are allowed to produce (AI Integration S4.2)."
        },
        "prohibitions": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/Prohibition"
          },
          "description": "Prohibitions forbidding specified agent outputs or actions (AI Integration S4.3)."
        },
        "obligations": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/Obligation"
          },
          "description": "Obligations requiring agents to include specified content (AI Integration S4.4)."
        },
        "rights": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/Right"
          },
          "description": "Rights specifying what agents are entitled to receive as input (AI Integration S4.5)."
        }
      }
    },
    "Permission": {
      "type": "object",
      "required": [
        "id",
        "onViolation"
      ],
      "additionalProperties": false,
      "properties": {
        "id": {
          "type": "string",
          "minLength": 1,
          "description": "Unique permission identifier.",
          "examples": [
            "outputFieldScope",
            "eligibilityRange"
          ]
        },
        "allowedFields": {
          "type": "array",
          "items": {
            "type": "string",
            "minLength": 1
          },
          "description": "Fields the agent is permitted to produce.",
          "examples": [
            [
              "eligible",
              "reason",
              "confidence"
            ]
          ]
        },
        "field": {
          "type": "string",
          "minLength": 1,
          "description": "Specific field this permission constrains."
        },
        "bounds": {
          "type": "string",
          "minLength": 1,
          "description": "FEL expression defining value bounds.",
          "examples": [
            "value = true or value = false"
          ]
        },
        "onViolation": {
          "$ref": "#/$defs/ViolationAction"
        },
        "nullBehavior": {
          "$ref": "#/$defs/NullBehavior"
        },
        "bypassable": {
          "type": "boolean",
          "default": false,
          "description": "Whether an authorized actor may bypass this constraint with structured rationale (AI Integration S4.7)."
        }
      }
    },
    "Prohibition": {
      "type": "object",
      "required": [
        "id",
        "condition",
        "onViolation"
      ],
      "additionalProperties": false,
      "properties": {
        "id": {
          "type": "string",
          "minLength": 1,
          "description": "Unique prohibition identifier.",
          "examples": [
            "noFinalDenial",
            "noInconsistentApproval"
          ]
        },
        "condition": {
          "type": "string",
          "minLength": 1,
          "description": "FEL expression defining the forbidden condition. When true, the prohibition is triggered.",
          "examples": [
            "output.eligible = false and instance.impactLevel = 'rights-impacting'"
          ],
          "x-lm": {
            "critical": true,
            "intent": "The guard expression that triggers this prohibition"
          }
        },
        "reason": {
          "type": "string",
          "description": "Human-readable explanation. Included in provenance when triggered.",
          "examples": [
            "Agent may not render final denial in rights-impacting workflow."
          ]
        },
        "onViolation": {
          "$ref": "#/$defs/ViolationAction"
        },
        "nullBehavior": {
          "$ref": "#/$defs/NullBehavior"
        },
        "bypassable": {
          "type": "boolean",
          "default": false,
          "description": "Whether an authorized actor may bypass this constraint with structured rationale (AI Integration S4.7)."
        }
      }
    },
    "Obligation": {
      "type": "object",
      "required": [
        "id",
        "requirement",
        "onViolation"
      ],
      "additionalProperties": false,
      "properties": {
        "id": {
          "type": "string",
          "minLength": 1,
          "description": "Unique obligation identifier.",
          "examples": [
            "citesRegulation",
            "perFieldConfidence"
          ]
        },
        "requirement": {
          "type": "string",
          "minLength": 1,
          "description": "FEL expression defining the required condition. When false, the obligation is unmet.",
          "examples": [
            "contains(output.reason, 'CFR') or contains(output.reason, 'USC')",
            "output.confidence.fieldLevel != null"
          ],
          "x-lm": {
            "critical": true,
            "intent": "The guard expression that checks this obligation"
          }
        },
        "reason": {
          "type": "string",
          "description": "Human-readable explanation.",
          "examples": [
            "Determination must cite regulatory authority."
          ]
        },
        "onViolation": {
          "$ref": "#/$defs/ViolationAction"
        },
        "nullBehavior": {
          "$ref": "#/$defs/NullBehavior"
        },
        "bypassable": {
          "type": "boolean",
          "default": false,
          "description": "Whether an authorized actor may bypass this constraint with structured rationale (AI Integration S4.7)."
        }
      }
    },
    "Right": {
      "type": "object",
      "required": [
        "id",
        "entitlement"
      ],
      "additionalProperties": false,
      "properties": {
        "id": {
          "type": "string",
          "minLength": 1,
          "description": "Unique right identifier.",
          "examples": [
            "receivesApplicationData",
            "receivesParameterValues"
          ]
        },
        "entitlement": {
          "type": "string",
          "minLength": 1,
          "description": "Path or reference specifying the data the agent is entitled to receive.",
          "examples": [
            "caseFile.application",
            "parameters.incomeThreshold"
          ]
        },
        "description": {
          "type": "string",
          "description": "Human-readable description.",
          "examples": [
            "Agent must receive application data as input."
          ]
        }
      }
    },
    "ViolationAction": {
      "type": "string",
      "enum": [
        "reject",
        "escalateToHuman",
        "switchToAssistive",
        "flag"
      ],
      "description": "Enforcement action for a deontic constraint violation (AI Integration S4.6). Restriction ordering: reject > escalateToHuman > switchToAssistive > flag."
    },
    "NullBehavior": {
      "type": "string",
      "enum": [
        "pass",
        "deny",
        "escalate"
      ],
      "description": "Override for null propagation behavior (AI Integration S4.9). When omitted, determined by the kernel's impactLevel."
    },
    "ConfidenceFloor": {
      "type": "object",
      "required": [
        "threshold",
        "onViolation"
      ],
      "additionalProperties": false,
      "properties": {
        "threshold": {
          "type": "number",
          "minimum": 0,
          "maximum": 1,
          "description": "Minimum acceptable confidence for the declared autonomy level.",
          "examples": [
            0.8,
            0.9,
            0.95
          ]
        },
        "onViolation": {
          "type": "string",
          "enum": [
            "escalateToHuman",
            "reject"
          ],
          "description": "Enforcement action when confidence falls below threshold."
        }
      }
    },
    "ConfidenceDecay": {
      "type": "object",
      "required": [
        "enabled"
      ],
      "additionalProperties": false,
      "properties": {
        "enabled": {
          "type": "boolean",
          "description": "Whether confidence decay is active."
        },
        "halfLife": {
          "type": "string",
          "description": "ISO 8601 duration after which effective confidence halves.",
          "examples": [
            "P14D",
            "P7D"
          ]
        },
        "triggers": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/DecayTrigger"
          },
          "description": "Events causing immediate confidence reduction."
        }
      }
    },
    "DecayTrigger": {
      "type": "object",
      "required": [
        "event",
        "decayFactor"
      ],
      "additionalProperties": false,
      "properties": {
        "event": {
          "type": "string",
          "minLength": 1,
          "description": "Event that triggers confidence decay.",
          "examples": [
            "caseFile.financials.modified",
            "caseFile.documents.added"
          ]
        },
        "decayFactor": {
          "type": "number",
          "minimum": 0,
          "maximum": 1,
          "description": "Multiplicative decay factor (0-1). Effective confidence is multiplied by this factor when the event fires.",
          "examples": [
            0.5,
            0.8
          ]
        }
      }
    },
    "FallbackLevel": {
      "type": "object",
      "required": [
        "action"
      ],
      "additionalProperties": false,
      "properties": {
        "action": {
          "type": "string",
          "enum": [
            "escalateToHuman",
            "retry",
            "alternateAgent",
            "fail"
          ],
          "description": "Fallback action type (AI Integration S8.3).",
          "examples": [
            "retry",
            "escalateToHuman"
          ],
          "x-lm": {
            "critical": true,
            "intent": "Determines what happens when an agent invocation fails"
          }
        },
        "taskRef": {
          "type": "string",
          "minLength": 1,
          "description": "Task reference for escalateToHuman."
        },
        "maxRetries": {
          "type": "integer",
          "minimum": 1,
          "description": "Maximum retry attempts for retry action.",
          "examples": [
            3
          ]
        },
        "backoff": {
          "type": "string",
          "enum": [
            "fixed",
            "exponential",
            "linear"
          ],
          "default": "exponential",
          "description": "Backoff strategy for retry action."
        },
        "initialInterval": {
          "type": "string",
          "description": "ISO 8601 duration for initial retry interval.",
          "examples": [
            "PT5S",
            "PT30S"
          ]
        },
        "alternateAgentRef": {
          "type": "string",
          "minLength": 1,
          "description": "Agent identifier for alternateAgent action."
        },
        "assignTo": {
          "type": "string",
          "minLength": 1,
          "description": "Actor to assign the escalated task to. Used with escalateToHuman action."
        }
      }
    },
    "OversightExtensions": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "suppressAgentOutput": {
          "type": "boolean",
          "default": true,
          "description": "When true and independentFirst protocol is active, hide agent output until the reviewer's independent assessment is recorded (AI Integration S10.2)."
        },
        "presentation": {
          "$ref": "#/$defs/OversightPresentation",
          "description": "Presentation properties for AI-assisted review steps."
        }
      }
    },
    "OversightPresentation": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "showConfidence": {
          "type": "boolean",
          "default": false,
          "description": "Display the agent's confidence score."
        },
        "showAlternatives": {
          "type": "boolean",
          "default": false,
          "description": "Display alternative recommendations."
        },
        "highlightLowConfidenceFields": {
          "type": "boolean",
          "default": false,
          "description": "Visually highlight fields with below-threshold confidence."
        },
        "showDiffFromIndependent": {
          "type": "boolean",
          "default": false,
          "description": "Highlight differences between reviewer's independent assessment and agent's recommendation. Only meaningful with independentFirst protocol."
        }
      }
    },
    "VolumeConstraints": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "maxAutonomousPerHour": {
          "type": "integer",
          "minimum": 0,
          "description": "Maximum autonomous agent actions per hour.",
          "examples": [
            100,
            50
          ]
        },
        "maxAutonomousPerDay": {
          "type": "integer",
          "minimum": 0,
          "description": "Maximum autonomous agent actions per day.",
          "examples": [
            500,
            200
          ]
        }
      }
    },
    "AgentReviewSampling": {
      "type": "object",
      "required": [
        "rate"
      ],
      "additionalProperties": false,
      "properties": {
        "rate": {
          "type": "number",
          "minimum": 0,
          "maximum": 1,
          "description": "Fraction of agent actions to sample for human review.",
          "examples": [
            0.1,
            0.25
          ]
        },
        "method": {
          "type": "string",
          "enum": [
            "random",
            "stratified",
            "adversarial"
          ],
          "default": "random",
          "description": "Sampling method (AI Integration S11.2). 'stratified': by agent type, confidence, or case characteristics. 'adversarial': targeted sampling of cases likely to reveal agent weaknesses."
        },
        "scope": {
          "type": "string",
          "enum": [
            "workflow",
            "agent"
          ],
          "default": "agent",
          "description": "Sampling scope."
        }
      }
    },
    "AgentDisclosure": {
      "type": "object",
      "required": [
        "discloseThatAgentAssisted"
      ],
      "additionalProperties": false,
      "properties": {
        "discloseThatAgentAssisted": {
          "type": "boolean",
          "description": "Disclose that an AI system assisted in the determination (AI Integration S12.2). MUST be true for rights-impacting workflows. Consistent with OMB M-24-10 and EU AI Act Art. 13.",
          "examples": [
            true
          ],
          "x-lm": {
            "critical": true,
            "intent": "Controls whether AI involvement is disclosed in adverse decision notices"
          }
        },
        "discloseModelIdentity": {
          "type": "boolean",
          "default": false,
          "description": "Disclose the model identifier."
        },
        "discloseConfidence": {
          "type": "boolean",
          "default": false,
          "description": "Disclose the agent's confidence score."
        }
      }
    },
    "NarrativeTierConfig": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "enabled": {
          "type": "boolean",
          "default": true,
          "description": "Whether agents should produce Narrative tier records."
        },
        "requiredForTags": {
          "type": "array",
          "items": {
            "type": "string",
            "minLength": 1
          },
          "description": "Transition tags that require Narrative tier records from agent actors.",
          "examples": [
            [
              "determination",
              "review"
            ]
          ]
        }
      }
    },
    "DriftDetectionConfig": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "trainingDataDisclosure": {
          "type": "boolean",
          "default": false,
          "description": "Whether agents must disclose training data characteristics (AI Integration S9.2)."
        },
        "optimizationObjectiveDisclosure": {
          "type": "boolean",
          "default": false,
          "description": "Whether agents must disclose their optimization objective."
        },
        "rubberStampMonitoring": {
          "$ref": "#/$defs/RubberStampConfig",
          "description": "Rubber-stamp detection configuration (AI Integration S9.2)."
        }
      }
    },
    "RubberStampConfig": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "enabled": {
          "type": "boolean",
          "default": false,
          "description": "Whether rubber-stamp detection is active."
        },
        "minReviewTime": {
          "type": "string",
          "description": "ISO 8601 duration below which a review is flagged as potentially rubber-stamped.",
          "examples": [
            "PT30S",
            "PT2M"
          ]
        },
        "maxAgreementRate": {
          "type": "number",
          "minimum": 0,
          "maximum": 1,
          "description": "Agreement rate above which reviewer engagement is flagged as declining.",
          "examples": [
            0.95,
            0.98
          ]
        },
        "evaluationWindow": {
          "type": "string",
          "description": "ISO 8601 duration of the evaluation window.",
          "examples": [
            "P30D",
            "P7D"
          ]
        }
      }
    },
    "AssistGovernanceProxy": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "enabled": {
          "type": "boolean",
          "default": false,
          "description": "Whether the WOS Assist Governance Proxy is active."
        },
        "toolCategories": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/ToolCategoryGovernance"
          },
          "description": "Per-tool-category governance configuration. The proxy consumes the Assist protocol (Assist S2.1, S7) and applies WOS deontic constraints."
        }
      }
    },
    "ToolCategoryGovernance": {
      "type": "object",
      "required": [
        "category"
      ],
      "additionalProperties": false,
      "properties": {
        "category": {
          "type": "string",
          "minLength": 1,
          "description": "Tool category identifier.",
          "examples": [
            "dataRetrieval",
            "computation",
            "externalApi"
          ]
        },
        "constraints": {
          "$ref": "#/$defs/DeonticConstraints",
          "description": "Deontic constraints specific to this tool category."
        },
        "maxInvocationsPerSession": {
          "type": "integer",
          "minimum": 0,
          "description": "Maximum tool invocations in this category per session."
        }
      }
    }
  }
}
