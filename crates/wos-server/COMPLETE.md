# wos-server — COMPLETE (archive)

Shipped **WS-*** work, sprint narratives, and completed checklist rows migrated from [`TODO.md`](TODO.md) on **2026-04-28**. Open backlog: [`TODO.md`](TODO.md). Spec-vs-impl matrix: [`PARITY.md`](PARITY.md).

---

## Sprint narratives (2026-04-25 — 2026-04-28)

## Completed — Phase 3 + HTTP coverage continuation (2026-04-28)

- **WS-036** Adverse-decision notice rendering (Gov §3.2) — `POST /api/governance/:url/notices/:template/render`. Joins notification template with `dueProcess` sidecar to stamp `gracePeriod`, `appealWindow`, `rightToContest`. Service: [`governance_service.rs::render_adverse_notice`](src/services/governance_service.rs). Reuses `find_template` / `interpolate` from `NotificationsService` (promoted to `pub(crate)`). Domain type: `AdverseDecisionNoticeView`. Tests: [`tests/http_adverse_notice.rs`](tests/http_adverse_notice.rs) (4 fixtures).
- **WS-014 slice C** HTTP coverage continuation — [`tests/http_coverage_slice_c.rs`](tests/http_coverage_slice_c.rs) (9 fixtures): verification/verify (inconclusive + 404), constraint-zones (list + valid-actions), equity/evaluate (Supervisor JWT + outcomePredicate→400), integration/invoke (Adjudicator echo + anonymous→401), assurance record→upgrade→chain (L1→L3 round-trip with `upgradedFrom` link + `chainValid`).
- **TODO.md follow-up #6** struck — `"Supervisor"` string literal absent in `realtime/` at HEAD.

## Completed — cross-phase parallel sprint (2026-04-25, second wave)

Seven items landed in parallel across seven background agents, plus three
reviewer agents using `formspec-specs:semi-formal-code-review`. Aggregate
review verdicts captured below; **read those before claiming any of these
items "done"** — several have blocker / request-changes findings the next
session must address.

- **WS-010** Timer poll e2e — APPROVE. Surfaced + fixed a real production
  timer-event bug along the way (PendingEvent envelope was missing
  `actorId` + `timestamp`; every fired timer was silently failing
  `serde_json::from_value` in `enqueue_event` and the runtime never saw
  the event — masked by `tracing::warn!`).
- **WS-011** Task draft/submit/dismiss tests — NEEDS DISCUSSION (2026-04-25
  review). Three fixtures pass. **2026-04-28 follow-up:** serde emits
  **camelCase** for `TaskSubmissionView::Completed`; `http_tasks_lifecycle`
  now asserts absence of snake_case keys. **(b) unchanged by design:**
  `dismiss_task` does not pop `active_tasks`; follow-up `/response` still
  succeeds — test documents current runtime until a spec/runtime change.
- **WS-012** Event submit/drain tests — REQUEST CHANGES (2026-04-25 review).
  **Retest 2026-04-28:** `http_event_submit_drain` **passes** at HEAD; if 503
  resurfaces, register the kernel through `services.bundle.replace(url, doc)`
  after `AppServices::new` so the bundle cache cannot diverge from `Storage`.
  Reviewer also flagged the test claims to lock `replay_entries` but
  actually exercises the HTTP-layer cache (`event_idempotency`) which
  short-circuits before the runtime is reached.
- **WS-014** HTTP coverage backfill — **addressed 2026-04-28** (slice A + partial B; remaining gaps tracked in [`TODO.md`](TODO.md) **Outstanding follow-ups** item 5).
  **Follow-up work:** [`tests/http_coverage_backfill.rs`](tests/http_coverage_backfill.rs)
  - [`tests/http_coverage_slice_b.rs`](tests/http_coverage_slice_b.rs) (shared
  [`tests/http_coverage_shared/`](tests/http_coverage_shared/)) pin `PARSE-001`, non-JSON lint body → **400**, `prov-o` **@context/@graph**,
  **xes** + **ocel** exports, bad `format` query → **400**, plus dashboard metrics keys.
- **WS-034** Policy resolve GET — **addressed 2026-04-28** (canonical `GET /api/policy/{url}/resolve?asOf=` + legacy POST; `resolve_policy` sorts by `effectiveDate` descending; `http_policy_resolve_get` out-of-order `versions` fixture). Original 2026-04-25 review items closed at HEAD.
- **WS-052** Session table hygiene — APPROVE. `Storage::sweep_expired_
  sessions` deletes 7d-expired + 30d-revoked rows; 24h cadence inside
  `timer_task::spawn`; `WOS_SESSION_SWEEP=off|false|0` opts out.
- **WS-082** Hold CRUD via typed HoldService — **closed 2026-04-28** (typed `HoldServiceError::NotFound` at HTTP/service boundary; `HOLD_NOT_FOUND_SENTINEL` is a private `hold_service` ↔ `update_instance_atomic` shim until `Storage` exposes a typed not-found path). Earlier review concern (public string sentinel) superseded — see **Closed this session** below.

**Reconciliation (2026-04-25 session — historical):**

- Restored `POST /api/auth/change-password` handler — was WIP at session
  start, never committed; the WS-002 close-out shipped four tests
  against a non-existent endpoint (reviewer FINDING 1, BLOCKER).
- Carried the `AppRuntimeConfig.bindings` field through after a
  multi-agent file race dropped it mid-session.
- *(Short SHAs cited in older drafts may not resolve after history rewrite; trust the narrative + current `main`.)*

**Closed this session:**

- **WS-014 slice A (HTTP backfill).** Extended `http_coverage_backfill` with
  PROV-O `@context`/`@graph`, invalid JSON + invalid export `format` → **400**.
- **WS-014 slice B (partial).** Split slice B into [`tests/http_coverage_slice_b.rs`](tests/http_coverage_slice_b.rs); shared code in [`tests/http_coverage_shared/`](tests/http_coverage_shared/). Conformance INT-CONSUME-001 + null fixture, calendar / notifications / integration profile via temp fixture tree, deontic + assurance + applicant + agents.
- **WS-082 string-sentinel → typed error.** Replaced
  `StorageError::Other("hold-not-found")` with
  `HoldServiceError::NotFound { index }` at the service boundary;
  `HOLD_NOT_FOUND_SENTINEL` is now a private impl detail of
  `services/hold_service.rs`. The `Mutex` workaround in
  `append`/`release` stays (the `update_instance_atomic` trait shape is
  the constraint) but is now annotated with the single-shot invariant;
  drop the sentinel entirely once `Storage` grows a typed-return
  variant (`update_instance_atomic_returns<T>`).

## Completed — Phase 1 sprint (2026-04-25)

Nine actionable items shipped this sprint, mostly Phase 1 + WS-080 / WS-083:

- **WS-057** README pagination semantics subsection.
- **WS-008** README realtime auth model subsection (kernel:update verify + studio reconnect rule).
- **WS-058** PARITY.md ▎ Auth contract + wos-spec/CLAUDE.md cross-link.
- **WS-009** realtime/mod.rs::on_connect rejects malformed/missing/expired JWT handshake at connect time.
- **WS-002** test coverage for the change-password endpoint (4 new fixtures, including hash-rotation invalidating in-flight access tokens).
- **WS-083** `Role` trait + `Supervisor` / `Adjudicator` / `Applicant` zero-sized markers + `RequireRole<R>` extractor on HTTP handlers; legacy `require_role(...)` call sites swept and the free function is `#[deprecated]` (prefer typed extractors — compile-time role strings).
- **WS-003** every mutator on instances / tasks / holds / agents / assurance / governance / equity / integration demands the typed Supervisor role; `/applicant/:id/appeal` is `RequireAuth`-gated until WS-091; `lint` / `notifications` / `calendar` / `conformance` POST routes remain anonymous (WS-014 / WS-091 territory). `tests/ws_003_auth_breadth.rs` spot-checks 6 representative routes.
- **WS-080** `AppRuntimeConfig { signer, renderer }` + `AppRuntime::build_with(...)` + `WOS_SIGNER` env wire-up; tests/ws_di_seams.rs proves the swap path. **Outstanding follow-up:** validator / access / external / clock seams stay hard-coded until upstream `Box<dyn>` blanket impls land in wos-core::traits — not blocking, fire when a real consumer needs to swap one of them.

Suite: **93** `#[test]` / `#[tokio::test]` cases in this crate (`rg` count, 2026-04-28); re-count after large merges. Theme commits this sprint: docs sprint → WS-003 close-out → WS-080 (plus smaller fixups).

## Completed — implementation sprint (2026-04-24)

- [✓] Explore wos-server crate structure and understand current codebase
- [✓] WS-051: Timer poll → use `list_instances_all_pages` (ROI 9.0, complexity 1)
- [✓] WS-001: `/ai/chat` Supervisor gate (ROI 6.0, complexity 1)
- [✓] WS-031: Chain-integrity verify endpoint (ROI 8.0, complexity 1)
- [✓] WS-024: Wire `ProvenanceSigner` seam (ROI 12.5, complexity 2)
- [✓] WS-025: Wire `ReportRenderer` seam (ROI 12.5, complexity 2)
- [✓] WS-026: `PolicyLayeredValidator` — tighten `ContractValidator` (ROI 12.5, complexity 2)
- [✓] WS-027: `RoleBasedAccessControl` — tighten `AccessControl` (ROI 12.5, complexity 2)
- [✓] WS-032: Event idempotency — HTTP-layer dedup (Restate handles natively)
- [✓] Compile-check and fix any issues
- [✓] Write tests for all 8 implemented items
- [✓] WS-005 / WS-007 / WS-022 / WS-056 — `WOS_CORS_STRICT` + `WOS_BEARER_STRICT`, `runtime_store` module docs, `Page::total` / `list_instances` drift docs + tests

## Completed — spec-surface sprint (2026-04-25)

- [✓] WS-029: `/instances/:id/explain` handler (ROI 12.5) — `assemble_explanation` from wos-core + ReportRenderer
- [✓] WS-013: `EvalService::available_transitions` parse-failure test (ROI 4.0)
- [✓] WS-037: Subject continuity-hash validation on assurance chain (ROI 3.0)
- [✓] WS-038: Calibration expiry enforcement on agent invocation (ROI 3.0)
- [✓] WS-039: SignatureAffirmation read surface (ROI 3.0) — endpoint already existed, test coverage confirmed
- [✓] WS-041: JSON-LD context endpoint (ROI 2.0)
- [✓] WS-053: `tasks` table marked reserved in migration
- [✓] WS-054: `equity_reports` table marked reserved in migration
- [✓] WS-035: Hold create / release CRUD (ROI 4.5) — JSON-shape mutators via `update_instance_atomic`, Supervisor-gated; full round-trip fixture

---

## Completed checklist rows (verbatim from former TODO.md)

- [✓] **WS-001 · `/ai/chat` Supervisor gate** `[3 / 2 / 1]` *(ROI 6.0)* — **Problem (pre-fix):** any authenticated caller could reach Gemini. **Done:** [`chat`](src/http/ai_chat.rs) takes `RequireRole<Supervisor>` (typed extractor alongside `State`); non-Supervisor → **403**, Supervisor reaches upstream; fixture covers both. **Gate:** none.
- [✓] **WS-002 · HTTP change-password (and/or admin reset)** `[4 / 4 / 3]` *(ROI 5.33)* — `POST /api/auth/change-password` is registered in [`http/auth.rs`](src/http/auth.rs) (see `change_password` handler in the same module); the 2026-04-25 work added coverage in [`tests/http_change_password.rs`](tests/http_change_password.rs) (wrong-current-password→400, short new password→400, anonymous→401, rotate-hash + invalidate-old-token round-trip). Studio wiring (`WosBackend` / `authedFetch`) deferred — separate package, not in scope for this crate. **Gate:** none.
- [✓] **WS-003 · HTTP auth breadth (2026-04-25)** `[4 / 4 / 3]` *(ROI 5.33)* — Every mutator on instances / tasks / holds / agents / assurance / governance delegations / equity / integration / events demands the right typed role through [`RequireRole<R: Role>`](src/auth/middleware.rs) (WS-083). The final write-side route `POST /api/applicant/:id/appeal` is `RequireAuth`-gated until per-actor scoping (WS-091) lands. **Carve-out:** `lint`, `notifications`, `calendar`, and `conformance` POST routes remain anonymous (WS-014 coverage / WS-091 actor-scoping territory) — the Supervisor-write rule does not apply to them. Spot-check fixture: [`tests/ws_003_auth_breadth.rs`](tests/ws_003_auth_breadth.rs) locks anon→401 + non-Supervisor→403 across drain / agents-register / equity-evaluate / applicant-appeal / integration-events-inbound / instances-identity-facts. **Gate:** none.
- [✓] **WS-005 · CORS startup mode** `[2 / 2 / 1]` *(ROI 4.0)* — Invalid `WOS_CORS_ORIGIN` today warns and falls back to `allow_origin(Any)` (see [`PARITY.md`](PARITY.md) ▎ Server aggregation + surface refresh). Add `WOS_CORS_STRICT=1` that fails fast at startup when the origin string is not a valid `HeaderValue`, for stricter deployments. **Done:** with strict set, an invalid origin aborts bind with a clear diagnostic; with it unset, the current warn-and-fallback behaviour is preserved. **Gate:** none.
- [✓] **WS-007 · Optional strict Bearer** `[2 / 2 / 2]` *(ROI 2.0)* — [`attach_auth`](src/auth/middleware.rs) treats invalid / expired `Authorization: Bearer` as anonymous today, which keeps single-user dev painless. Add `WOS_BEARER_STRICT=1` config flag that, when set, returns 401 on malformed or expired bearer headers instead of falling back to anonymous. Default preserves the anonymous fallback. **Done:** with `WOS_BEARER_STRICT=1` set, a request with a corrupt bearer gets 401; with it unset, same request reaches the handler as anonymous. **Gate:** none.
- [✓] **WS-008 · JWT Socket.IO — docs alignment (2026-04-25)** `[2 / 1 / 1]` *(ROI 2.0)* — README.md "Realtime auth model" subsection now names the kernel:update verify path and the studio's reconnect obligation after logout / role change / password reset. **Gate:** none.
- [✓] **WS-009 · JWT Socket.IO — early connect reject (2026-04-25)** `[2 / 2 / 2]` *(ROI 2.0)* — `realtime/mod.rs::on_connect` now disconnects immediately when JWT is configured AND the handshake token is missing or fails verify, instead of caching `None` and surfacing the failure on the first `kernel:update`. Verified by code inspection; full Socket.IO harness is WS-019 (soft, fires on regression). **Gate:** none.
- [✓] **WS-010 · #63 Timer poll end-to-end** `[4 / 4 / 3]` *(ROI 5.33)* — **Done:** [`tests/timer_poll_e2e.rs`](tests/timer_poll_e2e.rs) drives `timer_task::tick_once` on real `AppState` + `AppRuntime` (expired deadline → enqueue → drain → configuration advance). [`tests/timer_list_pagination.rs`](tests/timer_list_pagination.rs) still covers paged `list_instances` only. **Gate:** none.
- [✓] **WS-011 · Task draft / submit / dismiss HTTP** `[3 / 3 / 2]` *(ROI 4.5)* — **Done:** [`tests/http_tasks_lifecycle.rs`](tests/http_tasks_lifecycle.rs) exercises `POST /api/tasks/{id}/draft`, `/response`, `/dismiss` through Axum ([`http/tasks.rs`](src/http/tasks.rs)). **Residual:** dismiss does not pop `active_tasks` (runtime-documented until spec/runtime change); extend when parent [`../../TODO.md`](../../TODO.md) **#66** adds typed replay bodies — **WS-075**. **Gate:** none.
- [✓] **WS-012 · Event submit + drain HTTP path** `[4 / 3 / 3]` *(ROI 4.0)* — **Done:** [`tests/http_event_submit_drain.rs`](tests/http_event_submit_drain.rs) covers `POST /api/instances/{id}/events` → `enqueue_event` + `drain_once`, `head_record`, idempotent replay via HTTP-layer dedup (`http_event_submit_drain` header documents WS-012). **`runtime_lifecycle`** remains create+fetch only. Review note: fixture primarily locks HTTP idempotency cache + drain path, not deep `replay_entries` in `SqliteRuntimeStore` alone. **Gate:** none.
- [✓] **WS-013 · `EvalService::available_transitions` parse-failure surface** `[2 / 2 / 1]` *(ROI 4.0)* — Kernel / instance JSON deserialisation errors now return `ApiError::ServiceUnavailable` carrying the offending id ([`services/eval_service.rs`](src/services/eval_service.rs)) instead of silently returning an empty transition list. Lock it with a test: seed a malformed `instance_json` row via `Storage`, call `EvalService::available_transitions`, and assert a 503-mapped error whose message contains the instance id. Prevents the F3 fix from regressing to silent-empty. **Gate:** none.
- [✓] **WS-022 · `SqliteRuntimeStore` naming** `[2 / 2 / 1]` *(ROI 4.0)* — `runtime_store.rs` implements `wos_runtime::RuntimeStore` over `StorageHandle`, not `Storage` itself. Pick the lean: add a module-level doc block to [`storage/runtime_store.rs`](src/storage/runtime_store.rs) clarifying the split (`Storage` = durable rows, `RuntimeStore` = runtime projection bridge over `StorageHandle`) rather than renaming — the type is already stable across a dozen call sites. **Done:** module doc names the two roles and points at one call site for each. **Gate:** none.
- [✓] **WS-024 · Wire `ProvenanceSigner` seam (Runtime §12.6)** `[5 / 5 / 2]` *(ROI 12.5)* — Trait defined in [`wos-core::traits`](../wos-core/src/traits/mod.rs); server never injects. Add a `NoopSigner` (spec-correct `attestation` block with empty signature) as the default impl, a `WOS_SIGNER=noop|ed25519-file|external` config switch on [`ServerConfig`](src/config.rs), and inject through [`AppRuntime::build`](src/runtime/mod.rs) alongside the other host interfaces. Every day unwired, consumers either sign provenance exports out-of-band in incompatible formats or skip signing entirely (no externally-verifiable chain). **Done:** `/provenance` exports carry an `attestation` block produced by the injected signer; an `Ed25519FileKeySigner` reference impl sits behind a feature flag (WS-043). Ranked #1 (ROI 25.0). **Gate:** none.
- [✓] **WS-025 · Wire `ReportRenderer` seam (Runtime §12.7)** `[5 / 5 / 2]` *(ROI 12.5)* — Trait defined in [`wos-core::traits`](../wos-core/src/traits/mod.rs); server never injects. Add a `JsonReportRenderer` default + config switch + `AppRuntime::build` wire-up. Unblocks [`/instances/:id/explain`](src/http/instances.rs) (WS-029) and every future rendered-output surface (COC, legal notices, audit certificates); without it each new use case grows its own ad-hoc renderer — [`applicant_service`](src/services/applicant_service.rs) is already one such. **Done:** seam wired; `/explain` handler compiles against the injected renderer. Ranked #1 tied (ROI 25.0). **Gate:** none.
- [✓] **WS-026 · `PolicyLayeredValidator` — tighten `ContractValidator` stub (Runtime §15.7 ledger-gating)** `[5 / 5 / 2]` *(ROI 12.5)* — Runtime §15.7 obligates that submits on `impactLevel ∈ {rights-impacting, safety-impacting}` carry `respondentLedgerRef` evidence. Today [`runtime/validator.rs::PermissiveValidator`](src/runtime/validator.rs) accepts anything, so every rights-impacting workflow deployed against this stub violates a spec MUST silently. Replace with a layered impl composing: Formspec contract check (delegated) + §15.7 ledger-gating guard + the #43 signature-class ↔ assurance binding when that spec-side work lands. Trait at [`wos-core::traits`](../wos-core/src/traits/mod.rs). **Done (§15.7 half):** rights-impacting submit without ledger evidence is rejected with a conformance-shaped diagnostic; `PermissiveValidator` ships only as a test double. **Done (#43 half):** signature-class ↔ assurance binding enforced — tracked independently when parent repo #43 closes on the spec side. Ranked #4 (ROI 12.5). **Gate:** none for the §15.7 half; #43 awaits spec-side closure.
- [✓] **WS-027 · `RoleBasedAccessControl` — tighten `AccessControl` stub (Gov §7.2 / AI §1.5 separation-of-duties)** `[5 / 5 / 2]` *(ROI 12.5)* — Gov §7.2 MUST-says an actor cannot review their own output (AI §1.5 cross-references informatively). [`runtime/access.rs::PermissiveAccessControl`](src/runtime/access.rs) returns `true` unconditionally. Replace with a role-backed impl that rejects review-tagged transitions when actor identity matches the reviewed artifact's author, and honours delegation chains per Gov §6. Trait at [`wos-core::traits`](../wos-core/src/traits/mod.rs). **Done:** self-review on a review-tagged transition is rejected end-to-end; delegation fixture confirms a delegate can review when the original actor cannot. Ranked #4 tied (ROI 12.5). **Gate:** none.
- [✓] **WS-029 · `/instances/:id/explain` handler (Runtime §9 / Gov §3.3)** `[5 / 5 / 2]` *(ROI 12.5)* — Runtime §9 specifies the deterministic explanation algorithm; Gov §3.3 specifies delivery (individualised / categorical / aggregate by impact level). Today [`services/applicant_service.rs`](src/services/applicant_service.rs) assembles a partial surface for the applicant-determination view. Add `GET /api/instances/:id/explain` that loads provenance + kernel, runs the Runtime §9.1 algorithm, and hands the payload to the injected `ReportRenderer`. **Done:** endpoint accepts `transitionId` + optional `tags` query params, calls `wos_core::explain::assemble_explanation`, renders via injected `ReportRenderer`, returns structured `Explanation` + rendered text. Fixture covers 404 for missing instance and empty explanation for instance without matching provenance. Ranked #7 (ROI 12.5). **Gate:** ~~WS-025 (`ReportRenderer` seam wired)~~ ✓ satisfied; ~~Runtime §9.1 deterministic algorithm lands in `wos-runtime`~~ ✓ exists in `wos-core::explain`.
- [✓] **WS-031 · Chain-integrity verify endpoint (Kernel §8)** `[4 / 2 / 1]` *(ROI 8.0)* — [`services/provenance_service.rs::verify_chain`](src/services/provenance_service.rs) helper exists with zero callers. Add `GET /api/instances/:id/provenance/verify` that wraps it and returns `{valid: bool, brokenAt: Option<seq>}`. Auditors want an explicit chain-valid response, not an implicit "write succeeded so the chain was fine." **Done:** endpoint returns `valid: true` on a clean instance and `valid: false` with the correct `brokenAt` after a tampered row. Ranked #6 (ROI 8.0). **Gate:** none.
- [✓] **WS-032 · Event idempotency on `POST /api/instances/{id}/events` (Runtime §4.3)** `[4 / 4 / 2]` *(ROI 8.0)* — [`PendingEvent.idempotency_token`](../wos-core/src/instance.rs) (declared in `wos-core::instance`, populated across [`wos-runtime/src/runtime.rs`](../wos-runtime/src/runtime.rs)) exists and surfaces through `DrainOnceResult.processed_event_token`, but [`drain.rs`](../wos-runtime/src/runtime/drain.rs) never dedupes on it for general event submission. Task submission and `invokeService` already replay idempotently via `ReplayKey` / integration step-results — only the plain-event path is exposed. Add a dedup check in `drain_once` that, given a matching `(instance_id, idempotency_token)` on the event queue or a persisted replay entry, returns the stored result without re-running the transition. **Done:** POSTing to `/api/instances/{id}/events` twice with identical `idempotencyToken` produces identical response bodies and exactly one provenance row, not two. Ranked #8 (ROI 8.0). **Gate:** none.
- [✓] **WS-034 · Policy-parameters as-of resolution (PolicyParam §1.3)** `[4 / 3 / 2]` *(ROI 6.0)* — **Done:** canonical **`GET /api/policy/{url}/resolve?asOf=ISO8601`** ([`http/governance.rs`](src/http/governance.rs) — `policy_resolve_get`) plus legacy **`POST /api/governance/{url}/policy-resolve`** body form; [`GovernanceService::resolve_policy`](src/services/governance_service.rs) sorts by `effectiveDate` descending before half-open interval pick (array order not load-bearing). Fixtures: [`tests/http_policy_resolve_get.rs`](tests/http_policy_resolve_get.rs). Ranked #11 (ROI 6.0). **Gate:** none.
- [✓] **WS-035 · Hold create / release CRUD (Gov §3.6)** `[3 / 3 / 2]` *(ROI 4.5)* — `GET /api/instances/:id/holds`, `POST …/holds`, and `DELETE …/holds/:hold_idx` wired in [`http/instances.rs`](src/http/instances.rs). Mutations Supervisor-gated; payloads manipulate `governanceState.activeHolds` via `update_instance_atomic` so partial / pre-existing instance JSON does not block hold lifecycle. `hold_state` defaults to `configuration[0]` when omitted. Tests in [`tests/ws_spec_gaps_2.rs`](tests/ws_spec_gaps_2.rs) cover empty list, anonymous-rejection, full create→list→release round-trip with explicit + defaulted `holdState`, and 404 for an out-of-range release index. Ranked #12 (ROI 4.5). **Gate:** none.
- [✓] **WS-037 · Subject continuity-hash validation (Assurance §3)** `[3 / 2 / 2]` *(ROI 3.0)* — [`GET /api/subjects/:ref/assurance-chain`](src/http/assurance.rs) returns ordered facts but does not prove the chain. Extend the response with a continuity-hash walk that returns `{chainValid: bool, brokenAt: Option<factId>}`. Continuity is the main assurance observable. **Done:** clean chain returns `true`; broken `upgradedFrom` reference returns `false` at the correct fact id. Response shape is `AssuranceChainResponse { facts, chainValid, brokenAt }`. Ranked #13 (ROI 3.0). **Gate:** none.
- [✓] **WS-038 · Calibration expiry enforcement (AI §5.3)** `[3 / 2 / 2]` *(ROI 3.0)* — `wos-agent-config` stores `calibrationExpiresAt` but nothing consults it. Add an on-invocation check (or periodic sweep) that caps agent autonomy when calibration has expired, surfaced as provenance when it fires. Safety feature for production agents. **Done:** `tool_invocation_check` rejects agents with expired `calibrationExpiresAt` in config; `transition_lifecycle` blocks activation when expired; both paths reference AI §5.3. Fixtures cover expired and non-expired agents. Ranked #14 (ROI 3.0). **Gate:** none.
- [✓] **WS-039 · SignatureAffirmation read surface (Signature §2.8)** `[3 / 2 / 2]` *(ROI 3.0)* — Emission is wired in [`wos-runtime` `runtime/signature.rs`](../wos-runtime/src/runtime/signature.rs) (`ProvenanceRecord::signature_affirmation` ~L449) + call path from [`tasks.rs`](../wos-runtime/src/runtime/tasks.rs) via provenance. **Done:** dedicated read at [`http/signature.rs`](src/http/signature.rs) + [`signature_service.rs`](src/services/signature_service.rs); `tests/signature_affirmations.rs` covers empty and mixed-provenance cases. [`PARITY.md`](PARITY.md) Signature §2.8. **Gate:** none.
- [✓] **WS-041 · JSON-LD context endpoint (Semantic §3)** `[2 / 1 / 1]` *(ROI 2.0)* — Add `GET /api/semantic/jsonld-context` that serves the static JSON-LD context document. Tiny but unblocks RDF consumers. **Done:** endpoint serves the context with the correct `@context` + IRI mappings at [`http/semantic.rs`](src/http/semantic.rs). Test verifies `wos`, `prov`, `instanceId`, and `recordKind` keys. Ranked #16 (ROI 2.0). **Gate:** none.
- [✓] **WS-051 · Timer poll → use `list_instances_all_pages`** `[3 / 3 / 1]` *(ROI 9.0)* — **Was:** duplicated paginated walk over `list_instances`. **Done:** [`timer_task::tick_once`](src/services/timer_task.rs) calls [`list_instances_all_pages`](src/storage/mod.rs) (~L64–68); session sweep timing occupies the top of `spawn` (~L40–54). `tests/timer_list_pagination.rs` unchanged. **Gate:** none.
- [✓] **WS-052 · Session table hygiene** `[3 / 3 / 2]` *(ROI 4.5)* — `Storage::sweep_expired_sessions` ([`storage/sqlite.rs`](src/storage/sqlite.rs)) deletes rows where `expires_at < now - 7d` OR (`revoked = 1 AND expires_at < now - 30d`). [`services/timer_task.rs`](src/services/timer_task.rs) `spawn` tracks `last_sweep_at` and runs the sweep at most once per 24h, gated by `cfg.session_sweep_enabled` (`WOS_SESSION_SWEEP=off|false|0` opts out; default on). Each sweep emits `tracing::info!(deleted_rows = …, "session sweep")`. Tests in [`tests/session_sweep.rs`](tests/session_sweep.rs) cover the seeded fixture (one fresh, one expired-8d, one revoked-31d, one revoked-5d → returns 2; survivors verified) plus the all-fresh no-op path. **Gate:** none.
- [✓] **WS-053 · `tasks` table vs presenter — mark deferred** `[2 / 2 / 1]` *(ROI 4.0)* — `0002_runtime_tables.sql` creates `tasks` for future SQL mirroring; [`SocketIoTaskPresenter`](src/runtime/mod.rs) emits Socket.IO only and does not populate the table; `wos-runtime` owns authoritative task state. **Done:** migration documents deferral (comments on `tasks` / presenter split); [`PARITY.md`](PARITY.md) row notes the deferral (wording may differ from literal “RESERVED” in SQL — match file on disk). **Gate:** a consumer needs SQL-queryable tasks beyond the Socket.IO feed.
- [✓] **WS-054 · `equity_reports` storage — mark reserved** `[2 / 2 / 1]` *(ROI 4.0)* — Table exists from `0002`; no `Storage` trait methods yet and no caller. **Done:** migration comments describe reserved / future L3 cache posture; PARITY row matches; implement trait + SQLite methods only when the L3 equity cache work (Advanced §3.3 scheduled runs) is funded. **Gate:** L3 equity scheduler lands.
- [✓] **WS-056 · `list_instances` pagination — accept count/page drift** `[2 / 3 / 3]` *(ROI 2.0)* — SQLite's `COUNT(*)` and the paged `SELECT … LIMIT/OFFSET` are separate statements, so `total` can drift under concurrent writes. Pick the lean: **accept the drift** for reference-tier dashboards; do not wrap in a snapshot transaction (SQLite `SERIALIZABLE` would serialise every paged read and trade correctness for throughput we do not need). **Done:** [`storage/sqlite.rs`](src/storage/sqlite.rs) `list_instances` carries a doc comment that the `total` is best-effort under concurrent writes, and the response doc on `Page<T>` matches. Revisit only if a stable-page API promise enters the contract. **Gate:** client requires stable pagination.
- [✓] **WS-082 · Hold CRUD via service (shape leak) (2026-04-25)** `[2 / 3 / 2]` *(ROI 3.0)* — `HoldService::{list, append, release}` lives in [`services/hold_service.rs`](src/services/hold_service.rs) and round-trips through typed `CaseInstance` / `GovernanceState::active_holds`. Out-of-range / missing-governance → **`HoldServiceError::NotFound`** at the service boundary → HTTP **404**; a private `HOLD_NOT_FOUND_SENTINEL` may still bridge `update_instance_atomic`'s `StorageError` channel until `Storage` exposes a typed not-found path. The three hold handlers in [`http/instances.rs`](src/http/instances.rs) call only service methods; default `holdState` (configuration[0]) via typed `CaseInstance` read in `create_hold`. `seed_instance_with_state` in [`tests/ws_spec_gaps_2.rs`](tests/ws_spec_gaps_2.rs) writes a full `CaseInstance` shape for the round-trip. **Follow-up:** same precedent in [`services/applicant_service.rs::file_appeal`](src/services/applicant_service.rs) on `caseState` — left out of WS-082 scope since the `appealLog` shape is not yet typed in `wos-core::instance`. **Gate:** none.
- [✓] **WS-083 · `RequireRole<R: Role>` extractor (2026-04-25)** `[2 / 2 / 2]` *(ROI 2.0)* — `Role` trait + `Supervisor` / `Adjudicator` / `Applicant` zero-sized markers + `RequireRole<R: Role>` extractor in [`auth/middleware.rs`](src/auth/middleware.rs). HTTP write paths use typed role extractors; legacy `require_role(...)` is `#[deprecated]` but still exported for one cycle. `put_kernel_requires_bearer_jwt` / `put_kernel_forbidden_for_non_supervisor` fixtures lock unauth/wrong-role on `put_kernel`. **Gate:** none.
- [✓] **WS-074 · `PARITY.md` DI seam table vs shipped code** `[2 / 3 / 1]` *(ROI 6.0)* — Top-of-file **DI seam rework** prose and the §DI seam status table still describe `ProvenanceSigner` / `ReportRenderer` as unwired and `ContractValidator` / `AccessControl` as permissive stubs in places. Refresh after each seam sprint so [`PARITY.md`](PARITY.md) matches shipped seams (WS-024–027, WS-031–032 landed 2026-04-24). **Done (2026-04-25):** all four seam rows updated; summary counts shifted (full 30→32, partial 13→14, none 16→13); ranked table dropped 6 closed rows; compounding-costs and high-value-gap sections struck closed items; matrix rows updated for Kernel §8, Runtime §4.3, Runtime §5.5, Runtime §12, AI §1.5 / Gov §7.2. No row claims unwired/stub for a seam that `AppRuntime::build` injects with a policy impl. **Gate:** none.
- [✓] **WS-057 · README: aggregation vs paginated APIs (2026-04-25)** `[2 / 2 / 1]` *(ROI 4.0)* — README.md "Pagination semantics" subsection names the two categories — caller-paginated cursor (`GET /api/instances`) vs server-side fleet-scan aggregates (tasks, dashboard, equity). Links to PARITY.md ▎ Server aggregation. **Gate:** none.
- [✓] **WS-058 · Auth semantics in PARITY / CLAUDE (2026-04-25)** `[2 / 2 / 2]` *(ROI 2.0)* — PARITY.md ▎ Auth contract (top of file) carries the four invariants (global logout, atomic password rotation, upsert-never-overwrites, realtime parity). wos-spec/CLAUDE.md "Key rules" links there. **Gate:** none.

## Done (historical rollups; migrated from TODO.md)

- **WS-074 · PARITY.md DI seam table refresh (2026-04-25)** — Updated 4 DI seam rows, summary counts, 5 matrix rows, ranked gap table (22→16 rows), compounding-costs section, and high-value gaps to reflect WS-024–027 and WS-031–032 shipping. PARITY aligned with shipped checklist rows (same migration as this file).
- **WS-071 · 2026-04-24 server sprint (WS-001, WS-024–027, WS-031–032, WS-051)** — Supervisor gate on `/ai/chat`; `list_instances_all_pages` in timer poll; `ProvenanceSigner` + `ReportRenderer` wired; `PolicyLayeredValidator` and `RoleBasedAccessControl` tightened; provenance verify endpoint; HTTP-layer event idempotency dedup; compile-check and tests for all eight. Sprint checklist: **Completed — implementation sprint** section above in this file.
- **WS-059 · JWT global logout / `auth_epoch`** — global logout bumps `users.auth_epoch`, revokes all `sessions` for the user, and embeds `auth_epoch` in claims so in-flight refresh cannot beat logout; `set_user_password_hash` does hash + epoch + session revoke in one txn; `upsert_user` on conflict updates profile fields only (preserves `password_hash` and `auth_epoch`). `JwtAuth::verify` builds `AuthUser` from the **stored user row** (not JWT claims) after epoch + session checks, so role/profile changes apply without waiting for token expiry. See [`README.md`](README.md) Auth + Storage sections.
- **WS-060 · JWT `POST /auth/logout`** — revokes using the Bearer access token (not bare `jti`).
- **WS-061 · `PUT /bundles/{url}/kernel`** — auth + Supervisor; Gemini key via `x-goog-api-key`; shared HTTP client for Gemini.
- **WS-062 · Realtime `kernel:update`** — under JWT, handshake `auth.token` is stored only when `verify` succeeds; **each** `kernel:update` re-verifies that token and requires Supervisor from the current user row; mock remains permissive for local studio. Regression: `verify_returns_database_role_after_upsert_without_auth_epoch_bump` in [`tests/auth_jwt.rs`](tests/auth_jwt.rs) (shared verify path); full Socket.IO e2e still optional (WS-019).
- **WS-063 · `list_instances_all_pages`** — Used for task list/get filters, dashboard metrics, and equity evaluate; tests `tests/storage_sqlite.rs` (`list_instances_all_pages_collects_beyond_single_sqlite_page`), `tests/equity_outcome_predicate.rs` (`equity_evaluate_counts_all_matching_instances_across_pages`), `tests/timer_list_pagination.rs`.
- **WS-064 · CORS warn-and-fallback** — Invalid `WOS_CORS_ORIGIN`: `tracing::warn!` + fall back to `allow_origin(Any)` (credentials not combined with that branch).
- **WS-065 · Seed logging** — Demo password is not logged at info; README documents `wos-dev`.
- **WS-066 · Runtime store aux codec — intake replay round-trip (F1, 2026-04-24 review)** — `ReplayOperation::AcceptIntakeHandoff` + `ReplayValue::Intake(IntakeAcceptanceDecision)` now survive `SqliteRuntimeStore::save_record` / `load_record`. Upstream Serde derives on `wos_runtime::intake` data types (`IntakeCaseIntent`, `IntakeCaseDefinition`, `IntakeCaseDisposition`, `IntakeAcceptanceRequest`, `IntakeAcceptanceOutcome`, `IntakeAcceptanceDecision`, `IntakeRecordStatus`, `IntakeRecord`) plus a matching decode branch in `aux_from_json`. Covered by `tests/runtime_store_persistence.rs::aux_replay_round_trips_intake_handoff_entries`.
- **WS-067 · Durable intake records (F4, 2026-04-24 review)** — `IntakeRecord` moved from an in-memory `HashMap` on `SqliteRuntimeStore` to an SQLite `intake_records` table (migration `0003_intake_records.sql`). New `IntakeRecordRow` + `Storage::{get,insert,update}_intake_record` wire the persistence path; `SqliteRuntimeStore::{create,load,save}_intake_record` serialise the record through `serde_json` and route through the trait. Covered by `tests/runtime_store_persistence.rs::intake_records_persist_across_store_handles`.
- **WS-068 · Runtime-store NotFound error context (F5, 2026-04-24 review)** — `storage_err_with(e, ctx)` in `storage/runtime_store.rs` threads the missing resource id (instance id or `binding:intake_id`) through `StorageError::NotFound → StoreError::NotFound(ctx)` instead of emitting an empty string. Covered by `tests/runtime_store_persistence.rs::not_found_error_preserves_resource_id`.
- **WS-069 · Dashboard synthetic marker (F7, 2026-04-24 review)** — `DashboardMetricsView.synthetic_fields: Vec<String>` enumerates which metric fields are reference-server fixtures (sla, avg processing time, ai acceptance, every `*Trend`) so consumers can distinguish stubs from measurements; `drift_data` docstring flags the series as synthetic. Replacement with real aggregations tracked in WS-055.
- **WS-070 · `EvalService::available_transitions` surfaces parse errors (F3, 2026-04-24 review)** — Kernel / instance JSON deserialisation failures return `ApiError::ServiceUnavailable` with the offending resource id in the message (`services/eval_service.rs`), instead of swallowing the error and returning an empty transition list. Regression test tracked in WS-013.
- **WS-077 · 2026-04-25 spec-surface sprint (WS-029, WS-013, WS-037, WS-038, WS-039, WS-041, WS-053, WS-054)** — Explain endpoint wired through `wos_core::explain::assemble_explanation` + injected `ReportRenderer`; eval-service parse-failure locked with 503 fixture; assurance chain returns `AssuranceChainResponse { facts, chainValid, brokenAt }` with `upgradedFrom` continuity validation; calibration expiry enforcement on `tool_invocation_check` + `transition_lifecycle` (AI §5.3); SignatureAffirmation read endpoint confirmed with existing tests; JSON-LD context endpoint at `/api/semantic/jsonld-context`; `tasks` and `equity_reports` migration tables documented as deferred / future cache. Suite was **78** tests at sprint close; **93** `#[test]` / `#[tokio::test]` in this crate at 2026-04-28 (`rg` count — re-verify after merges).
- **WS-078 · Hold CRUD endpoints (WS-035, 2026-04-25)** — `GET /api/instances/:id/holds`, `POST …/holds`, `DELETE …/holds/:hold_idx` in [`http/instances.rs`](src/http/instances.rs). Mutations Supervisor-gated; payload manipulates `governanceState.activeHolds` via `update_instance_atomic` so partially populated instance JSON does not block hold lifecycle. `hold_state` defaults to `configuration[0]`. Four fixtures in [`tests/ws_spec_gaps_2.rs`](tests/ws_spec_gaps_2.rs) cover empty list, anonymous-rejection, full create→list→release round-trip with explicit + defaulted `holdState`, and 404 for an out-of-range release index.
- **WS-079 · Test-fixture repair after WS-003 auth tightening (2026-04-25)** — In-flight WS-003 work added Supervisor gates on `POST /api/instances` and `POST /api/agents/:id/lifecycle-transition`; two pre-existing fixtures stopped exercising their original assertions because they were short-circuiting on 401. [`tests/runtime_lifecycle.rs::create_instance_via_http_roundtrips_through_runtime`](tests/runtime_lifecycle.rs) now sends `Authorization: Bearer mock-access` (mock auth's `verify` accepts any token and returns the default Supervisor user). [`tests/ws_spec_gaps_2.rs::lifecycle_activation_blocked_when_calibration_expired`](tests/ws_spec_gaps_2.rs) uses `supervisor_bearer(&state)` to log in as the seeded Supervisor before posting. Both tests now reach their original assertions; full crate suite is green (zero failures across `cargo test --no-fail-fast`).
