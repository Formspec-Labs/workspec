{
  "id": "K-045-timer-tolerance-violation",
  "rule": "K-045",
  "description": "A timer whose tolerance (PT30S) exceeds its duration (PT5S) is a conformance violation (LCD S6.6, Runtime S7.2). The processor MUST emit a diagnostic provenance record indicating the violation.",
  "documents": {
    "kernel": "../../../../fixtures/kernel/timer-tolerance-violation.json"
  },
  "initial_case_state": {},
  "event_sequence": [],
  "expected_transitions": [],
  "expected_provenance": [
    {
      "record_kind": "timerCreated",
      "data": { "timerId": "shortTimer" }
    }
  ],
  "expected_errors": [
    "tolerance exceeds duration"
  ]
}
