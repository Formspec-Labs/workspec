// Rust guideline compliant 2026-02-21

//! `WosProject` — the high-level authoring facade (stub).
//!
//! Task 6 populates this with the 28 intent-driven helper methods.
//! For now it exists only so that `lib.rs` can re-export the name.

/// Intent-driven authoring facade for WOS Kernel Documents.
///
/// All mutations go through helper methods; no raw command access is exposed.
/// This stub is populated in Task 6.
#[derive(Debug)]
pub struct WosProject;
