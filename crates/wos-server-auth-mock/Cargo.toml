[package]
name = "wos-server-auth-mock"
version = "0.1.0"
edition = "2024"
license = "Apache-2.0"
repository = "https://github.com/formspec/formspec"

[dependencies]
async-trait = "0.1"
chrono = { version = "0.4", features = ["serde"] }
wos-server-ports = { path = "../wos-server-ports" }
