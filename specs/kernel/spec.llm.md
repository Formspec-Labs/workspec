<!--
WOS Kernel Spec — LLM-optimized summary

PLACEHOLDER. The synthesis crates (`crates/wos-synth-*`) embed this
file at compile time as `KERNEL_SPEC_SUMMARY` for prompt context;
the file must exist for those crates to build. Production content
(an LLM-friendly distillation of `specs/kernel/spec.md` covering
the lifecycle / state machine / actors / case file / contracts)
will be authored when synthesis prompts move past Stage-1 (current
stage: prototype loop with empty kernel context).

For the canonical kernel spec, see `specs/kernel/spec.md`.
-->

# WOS Kernel — LLM Reference (placeholder)

This is a placeholder. Authoring an LLM-optimized kernel summary is
queued for the synthesis-loop maturation track (Stage-1 → Stage-2
prompt-context expansion).
